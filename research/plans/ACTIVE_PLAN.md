# ACTIVE_PLAN

goal: 以 CARLA-Air Dataset Generation Pipeline V1 为当前主线，先把 Town10HD `node01-node05` 的固定入口、统一训练索引、以及未来两 camera-node 部署所需的部署型切分规则定下来；再按证据顺序补齐真实 UE/CARLA import 与 runtime readback，之后才决定是否把 capture 接入更真实的 synthetic annotation 和 4D geometry pipeline。视频仍只用于组会展示和人工质检，不进入正式 pipeline。

current_milestone: CARLA-Air Dataset Generation Pipeline V1 设计与证据补齐阶段。已完成 `node01-node05` 布设、RGB capture smoke、默认 drone trajectory smoke baseline、presence gate smoke、review summary/overlay 质检闭环、4A 同步采集器 live smoke、coverage-first trajectory generator、4B 正式数据契约冻结、代表样例的 node 级 `scene_dir` skeleton 导出、`traj_poc_03_southeast_pass` / `traj_poc_04_north_bridge` 的 live coverage 补强采集、gate 与 scene skeleton 导出、POC03 node03/node04 的最小正式样本闭环验证，以及 POC03 node03/node04 的 live synthetic target-mask + non-proxy depth-backprojection ReID smoke。当前 V1 不是 immediate proxy/NeoVerse replacement 阶段；它要求先完成 `mask_gt` availability audit、真实 UE/CARLA import 证据、runtime readback、再判断 pseudo/no-mask handling。2026-05-28 新增的只读 mask-quality diagnostic audit 对两个 POC03 scene 都触发强风险；同日 target actor binding probe 证明 recorded drone pose 可稳定绑定到 CARLA `airsim.drone` actor / AirSim `SimpleFlight`，但 AirSim segmentation id binding 仍失败。随后新增 actor-pose projected bbox candidate 工具，node03 可导出独立 `carla_actor_pose_projected_bbox` 候选 annotation / debug points / node-only embedding smoke，但 node04 当前窗口 projected bbox outside，且该候选不是 pixel-accurate target mask、identity proof 或正式 geometry。2026-05-29 actor-id instance encoding 审计进一步显示：在当前 POC03 node03/node04 前 10 帧、三路相机、6 种 packed instance-id 编码假设下，CARLA actor id `24` 没有出现在 `instance_synth` PNG 中；node03 ROI 仍只支持 `44800` 是几何候选。同日 semantic-lidar actor-index 诊断显示 close-offset `sensor.lidar.ray_cast_semantic` 可命中 `object_idx=24`，但固定三相机位置未命中；随后新增 isolated actor points export，POC03 node03/node04 前 10 帧分别导出 `carla_semantic_lidar_actor_idx_v1` actor-level points candidate，并新增只读 candidate verifier，确认两组 root 文件结构、sha256 lineage 与 diagnostic guard 自洽，`ok=true`、`failure_count=0`；但它仍是 actor-relative diagnostic geometry 支线，不是 camera mask、fixed-camera capture geometry、NeoVerse reconstruction、正式 `points_by_timestamp` 或最终 real 4D geometry。随后 close actor-relative instance/semantic camera binding probe 显示 POC03 node03/node04 的 actor ROI 内均稳定出现 `44800` 小区域和 `5387` 大区域，增强了 `44800` 的几何候选地位，但仍没有证明 CARLA actor id `24` 到 packed instance id 的映射。本地 CARLA Python API 未发现 actor-id -> instance-PNG value 映射或 setter，AirSim 近邻 object segmentation set/get 对默认 drone 相关对象也未生效。随后为 raw synthetic export / inspect / convert 增加保守 row-window 能力，并在 `local/` 下隔离副本中补跑 node04 后段 fixed-camera synthetic sensor；`start_index=93, limit=20` 覆盖 `84921711-86821711`，bbox ROI 审计显示 `44800` 在 node04 fixed-camera 后段也是最强几何候选，但这仍不是 actor-id mapping proof、pixel-accurate mask、正式 `masks_synth` 或 real 4D geometry。随后 `export_actor_pose_projected_bbox_annotations.py` 增加同基准 `--start-index`，并在同一 `local/` 隔离副本导出 node04 后段 `carla_actor_pose_projected_bbox` 独立候选：15 个有效 timestamp / 45 张 `masks_actor_bbox_later_window`，仍不更新正式 contract、不写 `masks_synth`、不构成 pixel-accurate mask、identity proof 或 real geometry。随后新增 weak variant downstream opt-in guard：`extract_node_track_embeddings.py` / `eval_node_track_retrieval.py` 现在对 `actor_bbox_semantic_lidar_diag`、`masks_actor_bbox`、`semantic_lidar_actor_points`、`carla_semantic_lidar_actor_idx_v1` 相关输入强制显式 `--weak-variant actor_bbox_semantic_lidar_diag` 与 pre-writer readiness JSON，并要求 weak eval 输出留在 `local/`；该 guard 只防默认误用，不是 protected real writer、promotion evidence 或正式 benchmark。当前重点不再是继续扩展示范视频，也不是把 `instance_id=5387`、`44800`、actor-bbox rectangle、close-camera ROI、later-window ROI 或 semantic-lidar close-offset points 叙述为 identity proof / pixel-accurate mask / final geometry，而是按 V1 先完成 UE/CARLA import 与 runtime readback 证据，再把数据契约、统一训练索引和两 camera-node deployment split 固化下来。

current_execution_conclusion: 2026-06-12 文档同步结论：2026-05-31 离线 Dataset Generation Pipeline V1 已作为最小离线闭环收束，核心 planner / runner / index builder / verifier / trajectory config 与 evidence reports 可以随本轮离线提交纳入；`ok=true`、`failure_count=0` 的离线 verifier 只证明 offline artifacts / schema / non-promotion contract 自洽。当前仍明确保持 `mask_gt_available_count=0`、`formal_mask_gt_ready=false`、`full_live_6_identity_dataset_complete=false`、`ue_carla_import_readback_complete=false`、`formal_annotation_ready=false`、`real_4d_geometry_ready=false`；不得把 default AirSim drone、proxy / candidate / pseudo / legacy masks、actor-bbox 或 semantic-lidar diagnostic 输出写成可信 `mask_gt`。

latest_actor_to_pixel_evidence_contract: 2026-05-29 新增 `research/reports/carla_air_actor_to_pixel_evidence_contract_2026_05_29_zh.md`。`local/carla_air/tmp/poc03_target_selection_readiness_actor_to_pixel_contract_20260529.json` 记录 `ok=true`、`target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`、`formalization_ready=false`、`goal_complete=false`，且 `required_actor_to_pixel_evidence.ready_for_target_selection=false` / `required_actor_to_pixel_evidence.satisfied=false`；`local/carla_air/tmp/target_traceability_gap_after_actor_to_pixel_contract_20260529.json` 继续保持 `actor_to_pixel_traceability_proven=false`、`formal_annotation_replacement_allowed=false`、`formal_points_replacement_allowed=false`、`can_replace_proxy_annotation_or_points_now=false`、`goal_complete=false`。这只是最新 gate 证据，不解除 proxy replacement blocker，也不改变 multi-identity benchmark readiness。
latest_goal_reset_dataset_generation_pipeline: 2026-05-30 记录 active stage reset 为 CARLA-Air Dataset Generation Pipeline V1。当前默认优先级变更为：先核对 `mask_gt` availability，再决定 pseudo/no-mask handling；保留历史 blocker 事实不变，包括 proxy annotation / proxy points 未替换、`target_selection_ready=false`、`formal_neoverse_ready=false`、UE/CARLA import missing。V1 的固定入口仍是 Town10HD `node01-node05`，但目标输出改为统一 training index + 面向未来 two-camera-node deployment 的 split 规则；full 6-identity live dataset generation 仍等待真实 UE/CARLA import 与 runtime readback。
latest_dataset_generation_pipeline_v1_scaffold: 2026-05-30 `research/reports/carla_air_dataset_generation_pipeline_v1_plan_index_scaffold_2026_05_30_zh.md` 同步了 V1 plan/index scaffold：`tools/carla_air/plan_dataset_generation_v1.py` 与 `tools/carla_air/build_dataset_training_index_v1.py` 已落到输出面，`local/carla_air/dataset_runs/dataset_v1_main_review_20260530/dataset_plan.json` 记录 `identity_count=6`、`trajectory_count=6`、`camera_layout_count=5`、`matrix_count=180`、`ue_carla_import_missing_count=6`；no-scene-root index 仍是 `sample_count=0`、`plan_only=true`，split names 为 `train` / `val_in_domain` / `test_cross_layout`，`deployment_episode_count=4`。2026-05-30 follow-up `research/reports/carla_air_dataset_pipeline_mask_gt_decoupling_2026_05_30_zh.md` 已使 existing-scene no-mask index 可物化：带 node04 POC03 scene root 的输出 `local/carla_air/dataset_runs/main_review_existing_scene_after_mask_decouple/dataset_v1_main_review_20260530/dataset_manifest.json` 为 `ok=true`、`plan_only=false`、`sample_count=360`，样本包含 RGB / depth / semantic / instance / pose / calib 引用，但 `mask_gt_availability_summary.available_count=0` 且 `mask_gt.is_mask_gt=false`。该 scaffold 仍未启动 live runtime，未完成 6-identity UE/CARLA import/readback，也未把 pseudo / candidate / proxy promotion 为 `mask_gt`。
latest_offline_v1_closure: 2026-05-31 `research/reports/carla_air_dataset_pipeline_offline_v1_closure_2026_05_31_zh.md` 收束了离线 Dataset Generation Pipeline V1：`local/carla_air/dataset_runs/dataset_v1_offline_v1_closure_main_review_20260531/` 的核心 artifact 均存在，`verification_json=local/carla_air/tmp/dataset_v1_offline_v1_closure_main_review_verification_20260531.json` 结果为 `ok=true`、`failure_count=0`、`warning_count=1`，唯一 warning 为 `identity_model_switch_mismatch_observed_scene_passthrough`，仅作为后续 live 阶段 blocker；本次明确不把 full 6-identity live dataset、UE/CARLA import/readback、可信 `mask_gt`、formal annotation、NeoVerse 或 real 4D geometry 作为当前完成 blocker。
latest_identity_switch_artifact_bridge: 2026-05-31 `research/reports/carla_air_dataset_pipeline_identity_switch_artifact_bridge_2026_05_31_zh.md` 记录了离线 Dataset Generation Pipeline V1 的 schema / index / verifier hardening：run 路径为 `local/carla_air/dataset_runs/dataset_v1_identity_switch_artifact_build_bridge_main_review_20260531/`，verifier 路径为 `local/carla_air/tmp/dataset_v1_identity_switch_artifact_build_bridge_main_review_verification_20260531.json`。build/index 阶段现已写出 standalone `identity_model_switch_manifest.json`，`run_contract.artifacts` 显式声明 `identity_model_switch_manifest_json`，verifier cross-check 该 standalone manifest 与 `dataset_manifest.identity_model_switch_contract`；结果为 `ok=true`、`failure_count=0`、`warning_count=1`，warning 仅为 expected observed scene passthrough identity mismatch。关键计数：`sample_count=2520`、`scene_count=7`、`capture_task_count=540`、`mask_gt_available_count=0`、`identity_mismatch_count=2520`、observed identity=`default_airsim_drone`、`sidecar_complete=60/2520`。本次明确 non-promotion：不启动 runtime，不构建或配置 UE，不设置 `ue_import_verified` / `carla_import_verified`，不宣称 full 6-identity live dataset complete，也不把 `default_airsim_drone` 或 proxy / candidate / pseudo / legacy masks 当作可信 `mask_gt` / formal annotation / real 4D geometry。
latest_sidecar_quality_artifact: 2026-05-31 `research/reports/carla_air_dataset_pipeline_sidecar_quality_artifact_2026_05_31_zh.md` 记录离线 Dataset Generation Pipeline V1 的 sidecar quality artifact hardening：run 路径为 `local/carla_air/dataset_runs/dataset_v1_sidecar_quality_artifact_main_review_20260531/`，verifier 路径为 `local/carla_air/tmp/dataset_v1_sidecar_quality_artifact_main_review_verification_20260531.json`。本次写出 standalone `sidecar_quality_manifest.json`，schema 为 `carla_air_sidecar_quality_manifest_v1`，并由 `dataset_manifest.outputs`、`run_contract.artifacts`、`batch_run_manifest.artifact_paths` 与 `artifact_manifest` 显式声明；verifier 结果为 `ok=true`、`failure_count=0`、`warning_count=1`，warning 仅为 expected `identity_model_switch_mismatch_observed_scene_passthrough`。关键计数：`sample_count=2520`、`scene_count=7`、`capture_task_count=540`、`sidecar_complete=60/2520`、missing depth/semantic/instance=`2460` each、`mask_gt_available_count=0`、`no_mask_sample_count=2520`、observed identity=`default_airsim_drone`、planned identities=`6`。本次明确 non-promotion：不启动 runtime / UE / CARLA，不设置 `ue_import_verified` / `carla_import_verified`，不宣称 full 6-identity live dataset complete，也不把 `default_airsim_drone` 或 proxy / candidate / pseudo / legacy `mask_gt` / actor-bbox / semantic-lidar candidates 当作可信 `mask_gt` / formal annotation / real 4D geometry。
latest_no_mask_non_promotion_manifest: 2026-05-31 `research/reports/carla_air_dataset_pipeline_no_mask_non_promotion_manifest_2026_05_31_zh.md` 记录离线 Dataset Generation Pipeline V1 的 standalone `no_mask_non_promotion_manifest.json` 硬化：run 路径为 `local/carla_air/dataset_runs/dataset_v1_no_mask_non_promotion_manifest_main_review_20260531/`，verifier 路径为 `local/carla_air/tmp/dataset_v1_no_mask_non_promotion_manifest_main_review_verification_20260531.json`。本次写出 standalone `no_mask_non_promotion_manifest.json`，schema 为 `carla_air_no_mask_non_promotion_manifest_v1`，并由 `dataset_manifest.outputs`、`run_contract.artifacts`、`batch_run_manifest.artifact_paths` 与 `artifact_manifest` 显式声明；verifier 结果为 `ok=true`、`failure_count=0`、`warning_count=1`，warning 仅为 expected `identity_model_switch_mismatch_observed_scene_passthrough`。关键计数：`sample_count=2520`、`scene_count=7`、`capture_task_count=540`、split `train=1440` / `val_in_domain=360` / `test_cross_layout=720`、`mask_gt_available_count=0`、`no_mask_sample_count=2520`、`sidecar_complete=60/2520`、missing depth/semantic/instance=`2460` each、observed identity=`default_airsim_drone`、planned identities=`6`、`identity_mismatch_count=2520`；样本抽查 2520 rows，`available=0`、`unavailable=2520`、`is_mask_gt_true=0`、`missing_policy_flag=0`、`legacy_flag_missing=0`；hashes 为 `core_digest_sha256=e5cf131b44a3b3737bdfec7eded5f1809015c90b9e67c866b3e0e5679e716c4c`、`manifest_digest_sha256=a405b2591c610fe5d7c6ef4c537133fcaa98c6131f837f75e0254a6aaec2d9de`。本次明确 non-promotion：不启动 runtime，不检查端口，不构建或配置 UE / CARLA，不设置 `ue_import_verified` / `carla_import_verified`，不宣称 full 6-identity live dataset complete，也不把 `default_airsim_drone` 或 proxy / candidate / pseudo / legacy 的 `mask_gt` / actor-bbox / semantic-lidar 候选当作可信 `mask_gt` / formal annotation / real 4D geometry。
latest_capture_queue_manifest: 2026-05-31 `research/reports/carla_air_dataset_pipeline_capture_queue_manifest_2026_05_31_zh.md` 记录离线 Dataset Generation Pipeline V1 的 standalone `capture_queue_manifest.json` 硬化：run 路径为 `local/carla_air/dataset_runs/dataset_v1_capture_queue_manifest_main_review_20260531/`，verifier 路径为 `local/carla_air/tmp/dataset_v1_capture_queue_manifest_main_review_verification_20260531.json`。本次写出 standalone `capture_queue_manifest.json`，schema 为 `carla_air_capture_queue_manifest_v1`，并由 `dataset_manifest.outputs`、`run_contract.artifacts`、`batch_run_manifest.artifact_paths` 与 `artifact_manifest` 显式声明；verifier 结果为 `ok=true`、`failure_count=0`、`warning_count=1`，warning 仅为 `identity_model_switch_mismatch_observed_scene_passthrough`。关键计数：`sample_count=2520`、`scene_count=7`、`capture_task_count=540`、`capture_queue_item_count=540`、`blocked_capture_queue_item_count=540`、`queued_capture_queue_item_count=0`、state_counts=`{blocked:540}`、block_reason_counts=`{await_ue_carla_import_readback_evidence:540}`、`scene_group_count=180`、split `train=1440` / `val_in_domain=360` / `test_cross_layout=720`、`mask_gt_available_count=0`、`no_mask_sample_count=2520`、`sidecar_complete=60/2520`、missing depth/semantic/instance=`2460` each、observed identity=`default_airsim_drone`、planned identities=`6`、`identity_mismatch_count=2520`；hashes 为 `capture_task_id_order_sha256=33e73472a65f50b12da14e4e28bd8bda2a3df566633777440e59a4cec78d3b77`、`expected_scene_root_order_sha256=fe40470dfe7d96a174bd82c4bc4def36af75754437ebe20a41f808af04f444ae`；artifact accounting 为 `artifact_count=24`、`contract_artifact_count=25`，self-reference gap 由 `artifact_manifest_json` 不纳入自身 hashed artifact entries 解释。本次明确 non-promotion：不启动 runtime，不检查端口，不构建或配置 UE / CARLA，不设置 `ue_import_verified` / `carla_import_verified`，不宣称 full 6-identity live dataset complete，也不把 `default_airsim_drone` 或 proxy / candidate / pseudo / legacy 的 `mask_gt` / actor-bbox / semantic-lidar 候选当作可信 `mask_gt` / formal annotation / real 4D geometry。
latest_existing_scene_index_bridge: 2026-05-31 `research/reports/carla_air_dataset_pipeline_existing_scene_index_bridge_2026_05_31_zh.md` 记录离线 Dataset Generation Pipeline V1 的 existing-scene index bridge hardening：run 路径为 `local/carla_air/dataset_runs/dataset_v1_existing_scene_index_bridge_main_review_20260531/`，verifier 路径为 `local/carla_air/tmp/dataset_v1_existing_scene_index_bridge_main_review_verification_20260531.json`。build/index 阶段现已写出 standalone `existing_scene_index_bridge_manifest.json`，schema 为 `carla_air_existing_scene_index_bridge_manifest_v1`，并由 `dataset_manifest.outputs`、`run_contract.artifacts`、`batch_run_manifest.artifact_paths` 与 `artifact_manifest` accounting 显式声明；verifier 对 schema、guard flags、run_id、scene/sample 计数、scene keys hash、scene split membership hash 与 scene entry 关键字段做 hard-check。结果为 `ok=true`、`failure_count=0`、`warning_count=1`，warning 仅为 expected observed scene passthrough identity mismatch。关键计数：`scene_root_count=7`、`indexed_scene_count=7`、`sample_count=2520`、`capture_task_count=540`、`mask_gt_available_count=0`、`no_mask_sample_count=2520`、`identity_mismatch_count=2520`、observed identity=`default_airsim_drone`、`sidecar_complete=60/2520`、missing depth/semantic/instance=`2460`。本次明确 non-promotion：不启动 runtime，不构建或配置 UE，不设置 `ue_import_verified` / `carla_import_verified`，不宣称 full 6-identity live dataset complete，也不把 `default_airsim_drone` 或 proxy / candidate / pseudo / legacy masks 当作可信 `mask_gt` / formal annotation / real 4D geometry。
latest_scene_sample_index_manifest: 2026-05-31 `research/reports/carla_air_dataset_pipeline_scene_sample_index_manifest_2026_05_31_zh.md` 记录离线 Dataset Generation Pipeline V1 的 scene-sample index manifest hardening：run 路径为 `local/carla_air/dataset_runs/dataset_v1_scene_sample_index_manifest_main_review_20260531/`，verifier 路径为 `local/carla_air/tmp/dataset_v1_scene_sample_index_manifest_main_review_verification_20260531.json`。build/index 阶段现已写出 standalone `scene_sample_index_manifest.json`，schema 为 `carla_air_scene_sample_index_manifest_v1`，并由 `dataset_manifest.outputs`、`run_contract.artifacts`、`batch_run_manifest.artifact_paths` 与 `artifact_manifest` 显式声明；该 manifest 将 scene -> sample -> split / identity / camera / timestamp membership 作为 verifier 可重计算的不变式。verifier 结果为 `ok=true`、`failure_count=0`、`warning_count=1`，warning 仅为 expected `identity_model_switch_mismatch_observed_scene_passthrough`。关键计数：`sample_count=2520`、`scene_count=7`、`capture_task_count=540`、split `train=1440` / `val_in_domain=360` / `test_cross_layout=720`、`mask_gt_available_count=0`、`no_mask_sample_count=2520`、`sidecar_complete=60/2520`、missing depth/semantic/instance=`2460` each、observed identity=`default_airsim_drone`、planned identities=`6`；hashes 为 `scene_split_membership_hash=6bebccfe5f586ca8ee7c43560e5074bd71f615f5d192083af0ea79dc772a04be`、`scene_sample_index_hash=24ba7e9b26e923e062dfff815c774fe64abbe3d8bb1b7b7431f2171f752ac6c2`、`scene_keys_sorted_hash=22e137d8ff006e7b9759228eb8a25f03b69a4b0d5638b66d027da2d0814bb35f`。本次明确 non-promotion：不启动 runtime / UE / CARLA，不设置 `ue_import_verified` / `carla_import_verified`，不宣称 full 6-identity live dataset complete，也不把 `default_airsim_drone` 或 proxy / candidate / pseudo / legacy `mask_gt` / actor-bbox / semantic-lidar candidates 当作可信 `mask_gt` / formal annotation / real 4D geometry。
latest_scene_sample_index_hash_bridge: 2026-05-31 `research/reports/carla_air_dataset_pipeline_scene_sample_index_hash_bridge_2026_05_31_zh.md` 记录离线 Dataset Generation Pipeline V1 的 cross-manifest hash bridge：run 路径为 `local/carla_air/dataset_runs/dataset_v1_scene_sample_index_hash_bridge_main_review_20260531/`，verifier 路径为 `local/carla_air/tmp/dataset_v1_scene_sample_index_hash_bridge_main_review_verification_20260531.json`。本轮把 `dataset_index_manifest.scene_sample_index_hash` 写入离线证据链，并由 verifier cross-check `dataset_index_manifest` 与 `scene_sample_index_manifest` 的 `scene_sample_index_hash` / `scene_count` / `scene_keys_sorted_hash` 一致性；`artifact_count=19`、`contract_artifact_count=20` 的 self-reference gap 仍按 `artifact_manifest_json` 解释。结果为 `ok=true`、`failure_count=0`、`warning_count=1`，warning 仅为 expected `identity_model_switch_mismatch_observed_scene_passthrough`。关键计数：`sample_count=2520`、`scene_count=7`、`capture_task_count=540`、split `train=1440` / `val_in_domain=360` / `test_cross_layout=720`、`mask_gt_available_count=0`、`no_mask_sample_count=2520`、`sidecar_complete=60/2520`、missing depth/semantic/instance=`2460` each、observed identity=`default_airsim_drone`、planned identities=`6`、`identity_mismatch_count=2520`；hashes 为 `dataset_index_manifest.scene_sample_index_hash=24ba7e9b26e923e062dfff815c774fe64abbe3d8bb1b7b7431f2171f752ac6c2`、`scene_sample_index_manifest.scene_sample_index_hash=24ba7e9b26e923e062dfff815c774fe64abbe3d8bb1b7b7431f2171f752ac6c2`、`scene_split_membership_hash=6bebccfe5f586ca8ee7c43560e5074bd71f615f5d192083af0ea79dc772a04be`、`scene_keys_sorted_hash=22e137d8ff006e7b9759228eb8a25f03b69a4b0d5638b66d027da2d0814bb35f`。本次明确 non-promotion：不启动 runtime / UE / CARLA，不设置 `ue_import_verified` / `carla_import_verified`，不宣称 full 6-identity live dataset complete，也不把 `default_airsim_drone` 或 proxy / candidate / pseudo / legacy `mask_gt` / actor-bbox / semantic-lidar candidates 当作可信 `mask_gt` / formal annotation / real 4D geometry。
latest_deployment_episode_visibility_manifest: 2026-05-31 `research/reports/carla_air_dataset_pipeline_deployment_episode_visibility_manifest_2026_05_31_zh.md` 记录离线 Dataset Generation Pipeline V1 的 standalone `deployment_episode_visibility_manifest.json` hardening：run 路径为 `local/carla_air/dataset_runs/dataset_v1_deployment_episode_visibility_manifest_main_review_20260531/`，verifier 路径为 `local/carla_air/tmp/dataset_v1_deployment_episode_visibility_manifest_main_review_verification_20260531.json`。本次 artifact 已由 `dataset_manifest.outputs`、`run_contract.artifacts`、`batch_run_manifest.artifact_paths` 与 `artifact_manifest` 显式声明；verifier hard-check schema、run_id、episode 计数、scene/sample visibility hash、episode entries 与 split policy cross-artifact digest。结果为 `ok=true`、`failure_count=0`、`warning_count=1`，warning 仅为 expected `identity_model_switch_mismatch_observed_scene_passthrough`。关键计数：`sample_count=2520`、`scene_count=7`、`capture_task_count=540`、split `train=1440` / `val_in_domain=360` / `test_cross_layout=720`、`mask_gt_available_count=0`、`no_mask_sample_count=2520`、`sidecar_complete=60/2520`、missing depth/semantic/instance=`2460` each、observed identity=`default_airsim_drone`、planned identities=`6`、`identity_mismatch_count=2520`。deployment visibility 计数为 `episode_count=4`、`sample_count_total_from_episodes=3240`、`scene_count_total_from_episodes=6`、`episode_with_visibility_gap_count=4`、`episode_without_samples_count=0`；hashes 为 `episode_scene_visibility_hash=8f05a2089f11f51ac782fd6582e027e01d1e5a6a64dee18e7c404abea84fc2ef`、`episode_sample_order_hash=ed891f48932f875f83beb39dd125240bffdc6773e511a740a6a6ccc02ae82500`、`episode_sample_sorted_hash=7575597e0a86641ea1b8871e50c3bc6a330bc3e3cbc342223af991cf5bb5257a`；split policy cross-artifact digest source count=`5`，digest=`731e99e5d3f7e09bd36aeac008ffd990fbc323e4c2fd7347b83e400636d34d0b`。本次明确 non-promotion：不启动 runtime / UE / CARLA，不设置 `ue_import_verified` / `carla_import_verified`，不宣称 full 6-identity live dataset complete，也不把 `default_airsim_drone` 或 proxy / candidate / pseudo / legacy `mask_gt` / actor-bbox / semantic-lidar candidates 当作可信 `mask_gt` / formal annotation / real 4D geometry。
latest_dataset_run_closure_manifest: 2026-05-31 `research/reports/carla_air_dataset_pipeline_run_closure_manifest_2026_05_31_zh.md` 记录离线 Dataset Generation Pipeline V1 的 standalone `dataset_run_closure_manifest.json` hardening：run 路径为 `local/carla_air/dataset_runs/dataset_v1_run_closure_manifest_main_review_20260531/`，verifier 路径为 `local/carla_air/tmp/dataset_v1_run_closure_manifest_main_review_verification_20260531.json`。本次 artifact 已由 `run_contract.artifacts`、`batch_run_manifest.artifact_paths` 与 `artifact_manifest` 显式声明；verifier hard-check schema、run_id、run_dir、artifact accounting、scene/sample/split/hash 计数、offline guard flags 与 stable hash。结果为 `ok=true`、`failure_count=0`、`warning_count=1`，warning 仅为 expected `identity_model_switch_mismatch_observed_scene_passthrough`。关键计数：`sample_count=2520`、`scene_count=7`、`capture_task_count=540`、`capture_queue_item_count=540`、`blocked_capture_queue_item_count=540`、split `train=1440` / `val_in_domain=360` / `test_cross_layout=720`、`mask_gt_available_count=0`、`no_mask_sample_count=2520`、`sidecar_complete=60/2520`、missing depth/semantic/instance=`2460` each、observed identity=`default_airsim_drone`、planned identities=`6`、sample-level `identity_mismatch_count=2520`。artifact accounting 为 `artifact_count=22`、`contract_artifact_count=23`，self-reference gap 由 `artifact_manifest_json` 不纳入自身 hashed artifact entries 解释；closure stable hash 为 `core_payload_sha256=8f74a92a0b066b14be237457d9c275f2a69c4617d41f921418a2c29001fb3992`。本次明确 non-promotion：不启动 runtime / UE / CARLA，不设置 `ue_import_verified` / `carla_import_verified`，不宣称 full 6-identity live dataset complete，也不把 `default_airsim_drone` 或 proxy / candidate / pseudo / legacy `mask_gt` / actor-bbox / semantic-lidar candidates 当作可信 `mask_gt` / formal annotation / real 4D geometry。
latest_sample_schema_coverage_manifest: 2026-05-31 `research/reports/carla_air_dataset_pipeline_sample_schema_coverage_manifest_2026_05_31_zh.md` 记录离线 Dataset Generation Pipeline V1 的 standalone `sample_schema_coverage_manifest.json` hardening：run 路径为 `local/carla_air/dataset_runs/dataset_v1_sample_schema_coverage_manifest_main_review_20260531/`，verifier 路径为 `local/carla_air/tmp/dataset_v1_sample_schema_coverage_manifest_main_review_verification_20260531.json`。本次 artifact 已由 `dataset_manifest.outputs`、`run_contract.artifacts`、`batch_run_manifest.artifact_paths` 与 `artifact_manifest` 显式声明；verifier hard-check schema、run_id、offline guard flags、required fields、field present/missing counts、stable hash，以及 standalone manifest 与 `dataset_manifest.sample_schema_coverage_summary` / verifier recomputed summary 的一致性。结果为 `ok=true`、`failure_count=0`、`warning_count=1`，warning 仅为 expected `identity_model_switch_mismatch_observed_scene_passthrough`。关键计数：`sample_count=2520`、`scene_count=7`、`capture_task_count=540`、split `train=1440` / `val_in_domain=360` / `test_cross_layout=720`、`mask_gt_available_count=0`、`no_mask_sample_count=2520`、`sidecar_complete=60/2520`、missing depth/semantic/instance=`2460` each、observed identity=`default_airsim_drone`、planned identities=`6`、`identity_mismatch_count=2520`；required fields `sample_id` / `scene_id` / `scene_key` / `identity_id` / `trajectory_id` / `node_id` / `camera_id` / `timestamp` / `split` / `rgb` / `pose` / `calib` / `depth` / `semantic` / `instance` / `mask_gt` 均为 `field_missing_count=0`。artifact accounting 为 `artifact_count=23`、`contract_artifact_count=24`，self-reference gap 仍由 `artifact_manifest_json` 不纳入自身 hashed artifact entries 解释；stable hash 为 `canonical_payload_sha256=9979c0a96171560d3c3c88caade4be9f3797aa0331debf81e6486f3b539b422a`。本次明确 non-promotion：字段存在不表示 sidecar 完整或 mask_gt 可信，不启动 runtime / UE / CARLA，不设置 `ue_import_verified` / `carla_import_verified`，不宣称 full 6-identity live dataset complete，也不把 `default_airsim_drone` 或 proxy / candidate / pseudo / legacy `mask_gt` / actor-bbox / semantic-lidar candidates 当作可信 `mask_gt` / formal annotation / real 4D geometry。
latest_ue_carla_import_externalization_plan: 2026-05-30 `research/reports/carla_air_ue_import_externalization_plan_2026_05_30_zh.md` 将 UE/CARLA import/readback blocker 改为外部机器交付项：本机没有可用 `UE4Editor` / `UE4Editor-Cmd` / `UnrealEditor` / `UnrealEditor-Cmd`，`/` 仅约 43G 可用，不适合构建 UE4.26/CARLA；`/mnt/windows_data2` 约 139G 可用但为 Windows/NTFS/FUSE 盘，不作为当前主线构建路径。当前长任务不得继续把“本机配置 UE4Editor”作为主线 blocker 消耗；本机继续推进离线 Dataset Generation Pipeline V1 的 verifier / schema / index / existing-scene no-mask 工作。完整 6-identity live dataset 仍 blocked，直到外部机器回传 per-identity UE asset 或 blueprint path、spawn/switch 方法、UE import smoke report、CARLA runtime readback report，以及可由 verifier/applier 检查的 evidence JSON 或 `build_status.json` patch。
latest_synthetic_annotation_evidence_contract_verifier: 2026-05-29 新增 `research/reports/carla_air_synthetic_annotation_evidence_contract_verifier_2026_05_29_zh.md`。`tools/carla_air/verify_synthetic_annotation_evidence_contract.py` 只是只读 future evidence contract verifier；`local/carla_air/tmp/synthetic_annotation_evidence_contract_main_review_blocked_20260529.json` 仍 `ok=false`、`formal_synthetic_annotation_ready=false`、`can_replace_proxy_annotation_now=false`、`goal_complete=false`、blocker `manifest_unavailable`，strict positive fixture 仅允许 strict fixture 场景，candidate / research-manifest negatives 继续拒绝。该 verifier 不替换 proxy annotation，不创建 real synthetic annotation，不写 scene outputs/contracts，不运行 CARLA / AirSim / UE / NeoVerse，也不完成 benchmark。

latest_objective_completion_audit: 2026-05-29 新增 `research/reports/carla_air_objective_completion_audit_2026_05_29_zh.md`。`tools/carla_air/verify_carla_air_objective_completion.py` 是只读 objective-completion auditor；正常输出 `local/carla_air/tmp/carla_air_objective_completion_audit_blocked_20260529.json` 为 `ok=true` 仅表示 audit integrity，且仍 `objective_complete=false`、`goal_complete=false`、五项 requirement 全为 `false`、`non_promotion_verified=true`；`--require-complete` 输出 `local/carla_air/tmp/carla_air_objective_completion_require_complete_blocked_20260529.json` 为 `ok=false`、`failure_count=1`、`failure=objective_incomplete_required`。这不启动 runtime / CARLA / AirSim / UE / NeoVerse，不写 scene / asset / contract，也不做 proxy replacement 或 benchmark promotion。

latest_actor_to_pixel_contract_downstream_gate_integration: 2026-05-29 新增 `research/reports/carla_air_actor_to_pixel_contract_downstream_gate_integration_2026_05_29_zh.md`。`tools/carla_air/audit_formal_annotation_path_preflight.py` 与 `tools/carla_air/audit_goal_execution_matrix.py` 现在都消费 target-selection JSON 中的 `required_actor_to_pixel_evidence`，默认输入对齐为 `local/carla_air/tmp/poc03_target_selection_readiness_actor_to_pixel_contract_20260529.json`。最新正式预检 `local/carla_air/tmp/formal_annotation_path_preflight_actor_to_pixel_contract_20260529.json` 仍 `ok=true`、`route_status.formal_conversion_allowed_now=false`、`route_status.formal_points_allowed_now=false`、`goal_complete=false`，且 summary 仍 `present=true` / `ready_for_target_selection=false` / `satisfied=false`；goal matrix `local/carla_air/tmp/goal_execution_matrix_actor_to_pixel_contract_20260529.json` 仍 `ok=true`、`can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`、`goal_complete=false`。这只是下游 gate integration，不放行 proxy replacement、real synthetic annotation、true final 4D geometry、NeoVerse 或 multi-identity benchmark。

latest_tracklet_input_contract_audit: 2026-05-29 新增 `tools/carla_air/audit_tracklet_input_contract.py` 只读 preflight。默认官方扫描输出 `local/carla_air/tmp/tracklet_input_contract_audit_20260529.json`：`ok=true`、`formalization_ready=false`、`goal_complete=false`、`tracklet_count=5`，分类为 2 个 `proxy_minimal_formal`、2 个 `synthetic_depth_backproject_smoke`、1 个 `actor_bbox_candidate`。加入本地 weak diagnostic tracklet 后输出 `local/carla_air/tmp/tracklet_input_contract_audit_with_weak_diag_20260529.json`：`tracklet_count=6`，新增 1 个 `weak_actor_bbox_semantic_lidar_diagnostic`。该审计不生成或修改 tracklet，不写 scene outputs，不进行 promotion；所有 tracklet 都保持 `ready_for_final_annotation=false` / `ready_for_final_geometry=false`，只能作为下游消费前的防误用分类证据。

latest_traceability_and_ue_import_guard_hardening: 2026-05-29 记录 `research/reports/carla_air_traceability_and_ue_import_guard_hardening_2026_05_29_zh.md`；`local/carla_air/tmp/target_traceability_gap_existing_actor_decode_summary_main_review_20260529.json` 为 `ok=true`、`actor_id=24`、`observation_count=12`、`capture_ok_count=12`、`decode_count=10`、`any_decode_roi_actor_id_positive=false`、`can_replace_proxy_annotation_or_points_now=false`、`goal_complete=false`，并且在传入 `--existing-actor-roi-decode-summary` 时 `evidence_summary` 只记录紧凑诊断摘要。`local/carla_air/tmp/aircraft_identity_ue_import_smoke_plan_verification_path_contract_20260529.json` 为 `ok=true`、`failure_count=0`、`verified_identity_plan_count=6`、`formal_benchmark_ready=false`、`goal_complete=false`、`non_promotion_verified=true`，并把 identity-scoped planned_report、精确 `assets/models/aircraft_normalized/<identity_id>/build_status.json` 和 patch / planned_report 对齐纳入核验。该硬化不证明 target actor-to-pixel binding，不创建真实 4D 几何，不运行 UE/CARLA editor import，也不把 multi-identity benchmark 变为可用。

latest_goal_resume_readiness_rerun: 2026-05-29 在不重置长任务的前提下重读 `docs/goal.md`、`AGENTS.md`、本计划与 handoff，并按“积极但保守”策略委派 actor-to-pixel 本地源码/API 只读 explorer 与独占报告 worker。续跑报告为 `research/reports/carla_air_goal_resume_readiness_rerun_2026_05_29_zh.md`：`local/carla_air/tmp/poc03_target_selection_readiness_rerun_20260529.json` 仍 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`、`formalization_ready=false`、`goal_complete=false`；`local/carla_air/tmp/candidate_formalization_readiness_rerun_20260529.json` 仍 `formalization_ready=false`、`goal_complete=false`；`local/carla_air/tmp/aircraft_identity_readiness_rerun_20260529.json` 仍 `ok=false`、`benchmark_ready=false`、`benchmark_eligible_count=0`。`local/carla_air/tmp/poc03_synth_plan_inspect_decode_diag_rerun_20260529.json` 是 plan-only，端口在线且 inspect-only 可执行不表示 target validation 或 formal conversion 就绪；没有新 target evidence 时不要仅因端口 open 就用 `--execute --stop-after inspect --overwrite` 改写官方 scene raw synthetic outputs。

latest_weak_variant_contract_update_execute: 2026-05-29 在 post-write verifier 保护下，已把 POC03 node03/node04 的 `actor_pose_projected_bbox_weak` 与 `semantic_lidar_actor_idx_weak` 登记到两个官方 `pipeline_contract.json` 的 `weak_diagnostic_variants` namespace。报告为 `research/reports/carla_air_weak_variant_contract_update_execute_2026_05_29_zh.md`；执行输出 `local/carla_air/tmp/weak_variant_contract_update_writer_execute_20260529.json` 为 `ok=true`、`written_update_count=2`、`formalization_ready=false`、`goal_complete=false`、`not_identity_proof=true`、`not_pixel_accurate_mask_evidence=true`、`not_real_or_final_geometry=true`。写后 verifier `local/carla_air/tmp/weak_variant_contract_update_verification_post_execute_20260529.json` 为 `ok=true`、`failure_count=0`、`registered_weak_variant_count=4`、`weak_marker_scene_count=2`，确认 weak marker 只在允许 namespace 内；聚合 readiness `local/carla_air/tmp/candidate_formalization_readiness_after_weak_contract_update_20260529.json` 仍 `formalization_ready=false`、`goal_complete=false`。该登记不是 `masks_synth` / `tracklets_synth` promotion，不是 identity proof、pixel-accurate mask、final geometry 或 NeoVerse reconstruction。

latest_weak_variant_downstream_contract_guard: 2026-05-29 在已执行 weak contract update 后，进一步收紧 downstream weak diagnostic 消费：weak path 同时要求 `--weak-variant-readiness` 与 `--weak-contract-verification`，并把 post-write verifier evidence 写入 embedding/eval 的 `weak_variant.contract_verification` metadata。`tools/carla_air/verify_weak_variant_diag_reid_smoke.py` 新增 contract evidence 检查与 `--embeddings-subdir`。报告为 `research/reports/carla_air_weak_variant_downstream_contract_guard_2026_05_29_zh.md`；正例 verifier `local/carla_air/tmp/weak_contract_guard_diag_reid_verification_20260529.json` 为 `ok=true`、`failure_count=0`，旧输出负例 `local/carla_air/tmp/weak_contract_guard_diag_reid_negative_old_outputs_20260529.json` 为 `ok=false`、`failure_count=8`。该 guard 只是 non-promotion / provenance check，不是 formal benchmark、identity proof、pixel-accurate mask、final geometry 或 goal completion。

latest_neoverse_geometry_readiness: 2026-05-29 新增只读 `tools/carla_air/audit_neoverse_geometry_readiness.py`，报告为 `research/reports/carla_air_neoverse_geometry_readiness_2026_05_29_zh.md`，输出 `local/carla_air/tmp/neoverse_geometry_readiness_20260529.json`：`ok=true`、`formal_neoverse_ready=false`、`goal_complete=false`、`writes_scene_outputs=false`、`updates_pipeline_contract=false`、`depth_backprojection_baseline_ready=true`、`depth_backprojection_is_neoverse_reconstruction=false`、`formal_neoverse_output_count=0`。审计确认 `mvp-demo/output/neoverse_fused` 当前两个 POC03 root 的 source 仍是 `carla_air_minimal_formal_proxy_box_surface_v1`，`mvp-demo/output/carla_air_depth_points` 两个 root 是 `carla_depth_synth_mask_backprojection_v1` baseline。2026-05-30 复核 `research/reports/carla_air_neoverse_checkpoint_recheck_2026_05_30_zh.md` 确认 `third_party/NeoVerse/models/NeoVerse/reconstructor.ckpt` 已通过软链接指向 `/mnt/windows_data2/4dreid_neoverse_weights/NeoVerse/reconstructor.ckpt`，目标文件大小 `6039277930` bytes；checkpoint 缺失 blocker 已移除，但 `local/carla_air/tmp/neoverse_geometry_readiness_checkpoint_recheck_20260530.json` 仍 `formal_neoverse_ready=false`，因为没有 formal NeoVerse points root，target selection / candidate formalization / multi-identity 仍未 ready。该审计不运行 runtime、不写 scene outputs，不把 proxy points、depth-backprojection baseline、semantic-lidar diagnostic points 或 weak diagnostic eval 升级为 NeoVerse/final real geometry。

latest_goal_execution_matrix: 2026-05-29 新增只读 `tools/carla_air/audit_goal_execution_matrix.py`，报告为 `research/reports/carla_air_goal_execution_matrix_2026_05_29_zh.md`，输出 `local/carla_air/tmp/goal_execution_matrix_20260529.json`：`ok=true`、`formalization_ready=false`、`goal_complete=false`、`writes_scene_outputs=false`、`updates_pipeline_contract=false`、`starts_runtime=false`、`can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`。矩阵把 `target_to_pixel_binding`、`formal_neoverse_geometry`、`multi_identity_benchmark` 标为 blocked，把 `weak_diagnostic_guarded_route` 标为 guarded diagnostic only；它只固化下一步命令与禁止动作，不执行 conversion、NeoVerse、benchmark 或 runtime。

latest_goal_execution_matrix_regression_visibility: 2026-05-29 新增 `research/reports/carla_air_goal_execution_matrix_regression_visibility_2026_05_29_zh.md`；`tools/carla_air/audit_goal_execution_matrix.py` 的 regression visibility 主审输出 `local/carla_air/tmp/goal_execution_matrix_regression_visibility_main_review_20260529.json` 为 `ok=true`、`route_count=5`、`blocked_route_ids=['target_to_pixel_binding','formal_neoverse_geometry','multi_identity_benchmark','proxy_replacement_readiness','weak_diagnostic_guarded_route']`、`goal_complete=false`，并保持 `can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`、`can_replace_proxy_annotation_or_points_now=false`；这只是 route-level regression 的诊断/可见性同步，不改变 blocked state。

latest_existing_actor24_decode_roi_sweep: 2026-05-29 新增 `tools/carla_air/probe_instance_id_calibration_actor.py` 的 existing actor 模式与 ROI sweep 参数，报告为 `research/reports/carla_air_actor24_existing_decode_roi_sweep_2026_05_29_zh.md`。live runtime 在 `Town10HD --opengl --quality Low --fg` 下，CARLA `2000` 与 AirSim `41451` 端口打开后运行；对 existing actor `24` / `airsim.drone` 的 6 视角 sweep（distance `4/8` x yaw `-20/0/20`）得到 `12` 次 observations、`12` 次 `capture_ok`，但 `contract_red_plus_256_green` 与 `green_plus_256_blue` 都在 projected ROI 中保持 `roi_actor_id_pixel_count=0`，只留下 `59927`、`44800`、`52202`、`61871` 这类 candidate / diagnostic value。`local/carla_air/tmp/poc03_target_selection_readiness_after_actor24_existing_sweep_20260529.json` 与 `local/carla_air/tmp/goal_execution_matrix_after_actor24_existing_sweep_20260529.json` 仍维持 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`、`goal_complete=false`、`can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`；这条线只强化 blocker，不放行 formal conversion、NeoVerse 或 benchmark。

latest_existing_actor_roi_sweep_target_readiness_integration: 2026-05-29 将 existing actor `24` ROI sweep evidence 接入 `tools/carla_air/audit_poc03_target_selection_readiness.py`，报告为 `research/reports/carla_air_existing_actor_roi_sweep_target_readiness_integration_2026_05_29_zh.md`。主审输出 `local/carla_air/tmp/poc03_target_selection_readiness_existing_actor_sweep_main_review_20260529.json` 显示 sweep 可读、`view_sweep_count=6`、`capture_count=12`、`capture_ok_count=12`，但 actor id `24` 在 projected ROI 下没有被任何 packed decode 或 single-channel component 命中；candidate formalization 与 traceability gap 复跑仍为 `formalization_ready=false`、`actor_to_pixel_traceability_proven=false`、`can_replace_proxy_annotation_or_points_now=false`、`goal_complete=false`。该集成只让 blocker 可复跑归因，不放行 formal conversion、NeoVerse/final geometry 或 benchmark。

latest_existing_actor_roi_decode_summary_integration: 2026-05-29 新增 `tools/carla_air/summarize_existing_actor_roi_decodes.py`，并让 `tools/carla_air/audit_poc03_target_selection_readiness.py` 接受 `--existing-actor-roi-decode-summary`，同时让 `tools/carla_air/run_post_guarded_traceability_audits.py` 在 guarded run output 中出现 existing-actor ROI sweep report 时自动生成 `existing_actor_roi_decode_summary.json` 再转交 target readiness。报告为 `research/reports/carla_air_existing_actor_roi_decode_summary_integration_2026_05_29_zh.md`；主审输出 `local/carla_air/tmp/existing_actor_roi_decode_summary_main_review_20260529.json`、`local/carla_air/tmp/poc03_target_selection_readiness_existing_actor_decode_summary_main_review_20260529.json`、`local/carla_air/tmp/post_guarded_existing_sweep_wrapper_fixture_20260529.json` 与其子文件均显示 `actor_id=24`、`target_mode=existing_actor`、`observation_count=12`、`capture_ok_count=12`、`decode_count=10`、`any_decode_roi_actor_id_positive=false`，因此 `target_selection_ready=false`、`formalization_ready=false`、`can_replace_proxy_annotation_or_points_now=false`、`goal_complete=false`。该集成只加强诊断链路与 wrapper 自动转递，不放行 formal conversion、NeoVerse/final geometry、benchmark 或 formal replacement。

latest_guarded_existing_actor_sweep_passthrough: 2026-05-29 guarded runner 现在接受 existing-actor sweep passthrough args，post-guarded wrapper 也会把 `existing_actor` reports 路由到 `--existing-actor-roi-sweep`，对应报告为 `research/reports/carla_air_guarded_existing_actor_sweep_passthrough_2026_05_29_zh.md`。本轮只是 no-runtime smoke，不启动 runtime、不停止 runtime，也不改变 `can_replace_proxy_annotation_or_points_now=false`、`goal_complete=false` 或 formal replacement 状态；该路径只是为下一次 gate 通过时保留可审计证据，不是正式 replacement。

latest_goal_execution_matrix_current_defaults: 2026-05-29 将 `tools/carla_air/audit_goal_execution_matrix.py` 默认输入对齐到当前权威 gate，并记录 `research/reports/carla_air_goal_execution_matrix_current_defaults_2026_05_29_zh.md`。默认 target-selection 输入改为 `local/carla_air/tmp/poc03_target_selection_readiness_controlled_decode_guard_20260529.json`，默认 identity 输入改为 `local/carla_air/tmp/aircraft_identity_readiness_current_multi_local_poc_20260529.json`，默认输出改为 `local/carla_air/tmp/goal_execution_matrix_current_defaults_20260529.json`。新默认矩阵为 `ok=true`、`target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`、`formal_neoverse_ready=false`、`benchmark_ready=false`、`benchmark_eligible_count=0`、`can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`、`goal_complete=false`；该改动只避免后续默认复跑读取较早 rerun JSON，不放行 formal conversion、NeoVerse/final geometry 或 formal multi-identity benchmark。

latest_runtime_stability_gate: 2026-05-29 新增 `tools/carla_air/audit_runtime_stability.py` 与报告 `research/reports/carla_air_runtime_stability_gate_2026_05_29_zh.md`，并把 runtime stability gate 接入 `tools/carla_air/audit_goal_execution_matrix.py --runtime-stability`。本轮 `local/carla_air/tmp/runtime_stability_audit_runtime_restart_attempt_20260529.json` 显示 `runtime_stable_for_window=false`、`sample_count=5`、`all_required_ports_open_sample_count=0`、`matching_process_seen_any_sample=false`，blockers 为 `required_ports_not_open_for_entire_window` / `no_matching_process_seen_during_window`；`local/carla_air/tmp/goal_execution_matrix_runtime_stability_gate_main_review_20260529.json` 仍给出 `runtime_stability_gate.status=blocked`、`can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`。`local/carla_air/tmp/poc03_target_selection_readiness_runtime_restart_attempt_20260529.json` 与 `local/carla_air/tmp/candidate_formalization_readiness_runtime_restart_attempt_20260529.json` 也仍为 false。该段只记录最新 blocked state，不表示完成、promotion 或 formal benchmark。

latest_fg_opengl_low_runtime_api_actor_binding: 2026-05-29 在 foreground OpenGL / Low 启动窗口下新增 live window 证据，报告为 `research/reports/carla_air_fg_opengl_low_runtime_api_actor_binding_2026_05_29_zh.md`。`local/carla_air/tmp/runtime_stability_audit_fg_opengl_low_api_probe_window_20260529.json` 记录 `sample_count=5`、`all_required_ports_open_sample_count=5`、`all_required_ports_open_all_samples=true`、`matching_process_seen_any_sample=true`、`runtime_stable_for_window=true`；`local/carla_air/tmp/runtime_api_responsiveness_audit_fg_opengl_low_ports_open_20260529.json` 记录 `carla_api_responsive=true`、`airsim_api_responsive=true`、`all_required_apis_responsive=true`。`local/carla_air/tmp/target_actor_binding_probe_fg_opengl_low_api_responsive_20260529.json` 记录 actor `24` / `airsim.drone` 与 AirSim `SimpleFlight` 的 pose 绑定可复现，但 `airsim_segmentation_set_effective_by_get_id=false`，`sufficient_for_identity_proof=false`，`sufficient_for_pixel_accuracy_proof=false`；`local/carla_air/tmp/carla_annotation_api_surface_probe_fg_opengl_low_20260529.json` 继续显示公开 CARLA Python API 没有 actor/world instance-id、segmentation-id 或 custom-stencil setter。该窗口只移除 foreground OpenGL / Low 启动模式下的即时 runtime TCP/API blocker，不改变 `target_selection_ready=false`、`ready_for_formal_conversion=false`、`formalization_ready=false` 或 `goal_complete=false`。

latest_runtime_evidence_reconciliation: 2026-05-29 对 target-selection readiness 与 goal execution matrix 做 runtime evidence 语义收敛，报告为 `research/reports/carla_air_runtime_evidence_reconciliation_2026_05_29_zh.md`。`tools/carla_air/audit_poc03_target_selection_readiness.py` 新增 `--runtime-stability` / `--runtime-api-responsiveness`，输出 `runtime_evidence_summary` 与 `runtime_notes`；`tools/carla_air/audit_goal_execution_matrix.py` 的 target route 也同步区分 current live ports 与 supplied runtime evidence gates。带 foreground OpenGL / Low 证据的 `local/carla_air/tmp/poc03_target_selection_readiness_runtime_evidence_reconcile_20260529.json` 为 `runtime_evidence_window_not_blocking=true` 但 `target_selection_ready=false` / `ready_for_formal_conversion=false`；`local/carla_air/tmp/goal_execution_matrix_runtime_evidence_reconcile2_20260529.json` 为 `runtime_stability_gate.status=not_blocking`、`runtime_api_responsiveness_gate.status=not_blocking`、`can_execute_formal_conversion_now=false`，target route runtime port 状态改入 non-blocking note。默认 regression `local/carla_air/tmp/goal_execution_matrix_default_reconcile_regression_20260529.json` 仍保持 runtime gates blocked。该改动只去掉误导性 runtime blocker，不放行 formal conversion、NeoVerse/final geometry 或 formal multi-identity benchmark。

latest_target_traceability_gap_audit: 2026-05-29 只读 target traceability gap 审计输出 `local/carla_air/tmp/target_traceability_gap_20260529.json`：`ok=true`、`missing_inputs=0`、`same_frame_world_geometry_evidence=true`、`actor_to_pixel_traceability_proven=false`、`formal_annotation_replacement_allowed=false`、`formal_points_replacement_allowed=false`、`can_replace_proxy_annotation_or_points_now=false`、`goal_complete=false`。该审计只证明 world-side gap audit 自洽，仍不能把 proxy annotation / proxy points 替换为正式 annotation 或 points。

latest_world_semantic_lidar_geometry_audit: 2026-05-29 world semantic-lidar geometry 审计输出 `local/carla_air/tmp/world_semantic_lidar_geometry_against_proxy_roots_20260529.json`：`pair_count=8`、`shared_timestamp_total_across_pairs=60`、比较根均为 `carla_world_xyz`、`coordinate_frames_match=true`。这是 diagnostic-only evidence，只强化 world-frame geometry 一致性，不修复 fixed-camera pixel masks、NeoVerse、final geometry 或 benchmark readiness。

latest_guarded_live_calibration_runner: 2026-05-29 guarded live calibration runner `tools/carla_air/run_guarded_live_calibration.py` 对应输出 `local/carla_air/tmp/guarded_live_calibration_no_runtime_main_review_20260529.json`：`diagnostic_only=true`、`starts_runtime=false`、`stops_runtime=false`、`gate_passed=false`、`runtime_stable_for_window=false`、`all_required_apis_responsive=false`、`probes_skipped=true`。该 runner 只保留受控探测入口，不构成 runtime 可用性证明，也不解除 formal conversion blocker。

latest_post_guarded_traceability_audits: 2026-05-29 新增 post-guarded 只读审计串联 `tools/carla_air/run_post_guarded_traceability_audits.py`，主审输出 `local/carla_air/tmp/post_guarded_traceability_audits_main_review_20260529.json`。该 wrapper 从 guarded-live summary 提取 `gates.runtime_stability.path` 与 `gates.runtime_api_responsiveness.path`，并传入 `audit_poc03_target_selection_readiness.py`，随后串联 candidate formalization 与 target traceability gap。结果仍为 `same_frame_world_geometry_evidence=true`、`actor_to_pixel_traceability_proven=false`、`formal_annotation_replacement_allowed=false`、`formal_points_replacement_allowed=false`、`can_replace_proxy_annotation_or_points_now=false`、`goal_complete=false`；它不启动/停止 runtime、不写 formal scene outputs、不更新 pipeline contract，也不把 proxy annotation / proxy points 晋升为正式产物。

latest_ue_import_explicit_editor_gate: 2026-05-29 为 UE/CARLA import readiness 与 smoke plan 增加显式 editor command gate，报告为 `research/reports/carla_air_ue_import_explicit_editor_gate_2026_05_29_zh.md`。`tools/carla_air/audit_aircraft_identity_ue_carla_import_readiness.py` 与 `tools/carla_air/plan_aircraft_identity_ue_import_smoke.py` 新增 `--editor-cmd`；显式命令必须存在、为文件、可执行，且 basename 为 `UnrealEditor-Cmd` / `UnrealEditor` / `UE4Editor-Cmd` / `UE4Editor`，否则记为 `explicit_unreal_editor_invalid`。负例 `local/carla_air/tmp/aircraft_identity_ue_carla_import_readiness_invalid_editor_arg_20260529.json` 与 `local/carla_air/tmp/aircraft_identity_ue_carla_import_readiness_bin_true_editor_arg_20260529.json` 均保持 `editor_available=false`；`/bin/true` 虽可执行但 `accepted_editor_name=false` / `usable_editor_command=false`。`local/carla_air/tmp/aircraft_identity_ue_import_smoke_plan_bin_true_editor_arg_20260529.json` 仍 `runnable_now_count=0`、`blocked_now_count=6`，verifier `local/carla_air/tmp/aircraft_identity_ue_import_smoke_plan_verification_bin_true_editor_arg_20260529.json` 为 `ok=true`、`non_promotion_verified=true`。该改动只把未来真实 Unreal Editor 路径接入 gate，不运行 Unreal、不写资产、不 patch `build_status.json`、不增加 `benchmark_eligible_count`。

latest_goal_resume_execution_status: 2026-05-29 继续按 `docs/goal.md` resume，报告为 `research/reports/carla_air_goal_resume_execution_status_2026_05_29_zh.md`。fresh audits 显示 `local/carla_air/tmp/goal_execution_matrix_resume_20260529.json` 仍 `can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`；`local/carla_air/tmp/poc03_target_selection_readiness_resume_20260529.json` 仍 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`；`local/carla_air/tmp/neoverse_geometry_readiness_resume_20260529.json` 仍 `formal_neoverse_ready=false`；`local/carla_air/tmp/aircraft_identity_readiness_resume_20260529.json` 仍 `ok=false`、`benchmark_ready=false`。runtime 端口在线后执行最小 GBuffer live diagnostic `local/carla_air/tmp/actor_gbuffer_binding_probe_node03_row0_actor_relative_resume_20260529/report.json`，row replay 成功且 actor `24` 对齐 recorded pose，但 `SceneStencil`、`CustomDepth`、`CustomStencil`、`SceneColor`、`SceneDepth`、`GBufferA` 均无 callback；复跑 `local/carla_air/tmp/poc03_target_selection_readiness_after_resume_gbuffer_20260529.json` 与 `local/carla_air/tmp/goal_execution_matrix_after_resume_gbuffer_20260529.json` 后三条正式路线仍 blocked。本轮不启动新 simulator、不执行 formal conversion、不改写官方 scene raw synthetic outputs。

latest_actor_raycast_visibility_probe: 2026-05-29 新增 diagnostic-only `tools/carla_air/probe_actor_raycast_visibility.py` 与报告 `research/reports/carla_air_actor_raycast_visibility_probe_2026_05_29_zh.md`。live 输出 `local/carla_air/tmp/actor_raycast_visibility_probe_node03_row0_20260529.json` 在 CARLA `2000` 与 AirSim `41451` 已在线时 replay POC03 node03 row0，actor `24` / `airsim.drone` 距 recorded pose 约 `0.000318677m`，`world_cast_ray=true`、`world_project_point=true`，三路 fixed camera 均有 projected bbox，24 个 sample 的 `cast_ray` / `project_point` 调用均成功；返回 label 为 `Buildings` / `None`，没有 actor id 字段。复跑 `local/carla_air/tmp/poc03_target_selection_readiness_after_raycast_20260529.json` 仍 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`；`local/carla_air/tmp/candidate_formalization_readiness_after_raycast_20260529.json` 仍 `formalization_ready=false`、`goal_complete=false`；`local/carla_air/tmp/goal_execution_matrix_after_raycast_20260529.json` 仍三条正式路线不可执行。该 probe 只用于 geometry/occlusion sanity，不写 masks、tracklets、points、status、contract、embedding 或 eval，不是 identity proof、pixel-accurate mask、formal synthetic annotation、NeoVerse reconstruction 或 final geometry。

latest_fg_opengl_low_instance_calibration_rerun: 2026-05-29 完成 `Town10HD --opengl --quality Low --fg` 下的受控车辆实例解码复测，报告为 `research/reports/carla_air_fg_opengl_low_instance_calibration_rerun_2026_05_29_zh.md`，probe 输出为 `local/carla_air/tmp/instance_id_calibration_actor_probe_fg_opengl_low_rerun_20260529/report.json`。结果仅证明 controlled actor `id=25` / `vehicle.tesla.model3` 在 `green_plus_256_blue` ROI 中可见，`roi_match_observation_count=3`、`roi_actor_id_pixel_count=9424`；formal contract `contract_red_plus_256_green` 对同一 ROI 仍为 `roi_actor_id_pixel_count=0`。这仍是 packed decode 诊断，不绑定 POC03 drone actor `24`，因此 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`、`goal_complete=false` 保持不变。`cleanup_remaining_after` 仍列出 3 个残留 actor/sensor，且 runtime session 后出现 Signal 11 崩溃/退出，只能记为 cleanup caveat，不作严格销毁证明。旧文档建议软归档、默认阅读集收敛，暂不物理移动。

latest_goal_continue_status: 2026-05-29 按 `docs/goal.md` 与“积极但保守”subagent 策略继续执行并刷新状态报告 `research/reports/carla_air_goal_continue_status_2026_05_29_zh.md`。fresh audits 显示 `local/carla_air/tmp/poc03_target_selection_readiness_goal_continue_20260529.json` 仍 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`；`local/carla_air/tmp/goal_execution_matrix_goal_continue_20260529.json` 与 read-only rerun 仍 `can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`；`local/carla_air/tmp/neoverse_geometry_readiness_goal_continue_20260529.json` 仍 `formal_neoverse_ready=false`、`formal_neoverse_output_count=0`，且现有 `neoverse_fused` roots 是 proxy source、`carla_air_depth_points` roots 是 depth-backprojection baseline；`local/carla_air/tmp/aircraft_identity_readiness_goal_continue_20260529.json` 仍 `benchmark_ready=false`、`normalized_inventory.benchmark_eligible_count=0`。`local/carla_air/tmp/poc03_synthetic_verification_goal_continue_latest_20260529.json` 虽 `ok=true`、`failure_count=0`，但只证明 POC03 synthetic depth-backprojection smoke lineage 自洽，不是 pixel-accurate identity proof、NeoVerse reconstruction 或 final real geometry。

latest_goal_active_audit: 2026-05-29 按当前 worktree 重新取证并记录 `research/reports/carla_air_goal_active_audit_2026_05_29_zh.md`。本轮只读 fresh audits 输出 `local/carla_air/tmp/poc03_target_selection_readiness_goal_active_20260529.json`、`poc03_synthetic_verification_goal_active_20260529.json`、`scene_pipeline_status_audit_goal_active_20260529.json`、`tracklet_input_contract_audit_goal_active_20260529.json`、`neoverse_geometry_readiness_goal_active_20260529.json`、`aircraft_identity_readiness_goal_active_20260529.json`、`candidate_formalization_readiness_goal_active_20260529.json` 与 `goal_execution_matrix_goal_active_20260529.json`。结果仍为 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`formal_neoverse_ready=false`、`benchmark_ready=false`、`can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`；POC03 synthetic verifier 仍 `ok=true` / `failure_count=0`，但只是 synthetic depth-backprojection smoke，不是 pixel-accurate identity proof、NeoVerse reconstruction 或 final geometry。

latest_annotation_api_surface_probe: 2026-05-29 新增 live diagnostic-only `tools/carla_air/probe_carla_annotation_api_surface.py`，报告为 `research/reports/carla_air_annotation_api_surface_probe_2026_05_29_zh.md`。在 CARLA `2000` 与 AirSim `41451` 已在线时输出 `local/carla_air/tmp/carla_annotation_api_surface_probe_20260529.json`：匹配 actor `24` / `airsim.drone`，actor annotation surface 只有 `semantic_tags` 且没有 actor-level setter；world 只暴露 `set_annotations_traverse_translucency`；`sensor.camera.instance_segmentation`、`sensor.camera.semantic_segmentation`、`sensor.camera.depth` 与 `sensor.lidar.ray_cast_semantic` blueprint 没有 instance / segmentation / custom / stencil 相关可设 attribute。随后 fresh gate `local/carla_air/tmp/poc03_target_selection_readiness_after_api_surface_20260529.json` 仍 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`，并新增 blocker：公开 CARLA Python API surface 没有 actor/world instance-id、segmentation-id 或 custom-stencil setter；`local/carla_air/tmp/candidate_formalization_readiness_after_api_surface_fresh_20260529.json` 仍 `formalization_ready=false`，`local/carla_air/tmp/goal_execution_matrix_after_api_surface_20260529.json` 为 `ok=true` 且三条正式路线仍不可执行。该 probe 不写 scene outputs、contract、masks、tracklets、points、embedding 或 eval，不是 identity proof、pixel-accurate mask、formal annotation、NeoVerse reconstruction 或 final geometry。

latest_goal_continue2_status: 2026-05-29 按 `docs/goal.md` 最新规则继续执行并记录 `research/reports/carla_air_goal_continue2_status_2026_05_29_zh.md`。本轮没有启动 simulator、没有执行 live export / capture / inspect、没有改写官方 scene raw synthetic outputs；只读 explorer `019e7120-27b0-78a3-9851-65f2a2d41b65` 未发现能推进正式路线的新证据，第二个只读 explorer `019e7126-6261-76b1-beeb-ca088b471ec5` 未在本地非 runtime 文本/脚本/报告中发现 actor id -> instance PNG value、segmentation id setter、custom stencil / material route 或 AirSim drone annotation binding。fresh audits 显示 `local/carla_air/tmp/poc03_target_selection_readiness_goal_continue2_20260529.json` 仍 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`；`local/carla_air/tmp/poc03_synthetic_verification_goal_continue2_20260529.json` 仍 `ok=true`、`failure_count=0`，但只证明 POC03 separate synthetic depth-backprojection smoke 自洽；`local/carla_air/tmp/scene_pipeline_status_audit_goal_continue2_20260529.json` 仍为 7 scenes / 2 synthetic smoke ready / 5 skeleton pending，且 candidate artifacts 不作为 formal inputs；`local/carla_air/tmp/candidate_formalization_readiness_goal_continue2_20260529.json` 仍 `formalization_ready=false`、`goal_complete=false`；修复后的 `local/carla_air/tmp/goal_execution_matrix_goal_continue2_20260529.json` 为 `ok=true`、`missing_or_unreadable=[]`，并继续显示 `can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`。本轮结论不变：`5387`、`44800`、bbox rectangle、semantic-lidar actor-relative points、depth-backprojection baseline、GBuffer / raycast diagnostics 与 weak route 都不能升级为 identity proof、pixel-accurate mask、formal annotation、NeoVerse reconstruction、final geometry 或 benchmark completion。

latest_actor_bbox_formalization_preflight: 2026-05-29 新增只读 `tools/carla_air/audit_actor_bbox_formalization_preflight.py`，报告为 `research/reports/carla_air_actor_bbox_formalization_preflight_2026_05_29_zh.md`，输出 `local/carla_air/tmp/actor_bbox_formalization_preflight_20260529.json`：`ok=true`、`can_continue_as_independent_bbox_annotation_variant=true`，但 `formal_annotation_replacement_ready=false`、`ready_for_masks_synth_or_tracklets_synth=false`、`identity_proof=false`、`pixel_accurate=false`、`real_or_final_geometry=false`、`goal_complete=false`。该 preflight 聚合 actor-bbox candidate verifier、target actor pose binding、target-selection gate 与 scene candidate policy；结论是 bbox route 可以继续作为独立非 pixel-accurate annotation variant 工程化，但仍不得写入 `masks_synth` / `tracklets_synth`，不得称为 identity proof、pixel-accurate mask、NeoVerse reconstruction、final geometry 或 benchmark evidence。同轮 multi-identity explorer 复核确认 `assets/models/aircraft_normalized/` 仍无真实 identity，`benchmark_eligible_count=0`；下一步只能先生成真实 normalized asset 后重跑 readiness gate，不能用 raw assets 或占位文件替代。

latest_current_worktree_reaudit: 2026-05-29 按当前 worktree 重新取证并记录 `research/reports/carla_air_current_worktree_reaudit_2026_05_29_zh.md`。本轮按仓库级 subagent 策略并行调用两个只读 explorer，分别核查 research/docs 侧与 `mvp-demo` / `tools/carla_air` / runtime artifacts 侧；两者与主 agent fresh audits 结论一致：POC03 node03/node04 separate synthetic depth-backprojection smoke 仍通过 verifier，但 primary `tracklets.json` 与 `mvp-demo/output/neoverse_fused/...` 仍是 proxy 主线，不能替换为正式 synthetic annotation 或 true 4D geometry。fresh outputs 为 `local/carla_air/tmp/*_current_session_20260529.json`；最终串行重跑的 `goal_execution_matrix_current_session_20260529.json` 为 `ok=true`、`missing_or_unreadable=[]`，并继续显示 `can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`。本轮没有启动 simulator、没有执行 live export / capture / inspect、没有改写官方 scene raw synthetic outputs、没有执行 formal conversion / NeoVerse / benchmark 写入。剩余缺口仍是可信 actor-to-pixel / target id evidence、formal NeoVerse checkpoint/root、以及真实 normalized benchmark identity。

latest_local_normalized_identity_poc: 2026-05-29 新增 `tools/carla_air/build_local_normalized_aircraft_identity.py`，尝试从 `assets/models/aircraft_raw/dji_drone_fbx_obj/extracted/modelNew.obj` 生成本地 normalized identity 三件套到 `assets/models/aircraft_normalized/dji_drone_fbx_obj_local_poc/`。报告为 `research/reports/carla_air_local_normalized_identity_poc_2026_05_29_zh.md`；生成的 `normalized.fbx`、`preview.png`、`asset_meta.json` 与 `build_status.json` 均已落盘，source mesh 为 49141 vertices / 81719 faces，bbox extent 为 `[0.4307517652777035, 0.9956017660663726, 0.9999999999999999]`。但 MeshLab readback `xvfb-run -a meshlabserver -i .../normalized.fbx -o .../normalized_poc_readback.obj -m vn` 发生 segmentation fault，且 license/benchmark permission 与 UE import 均未验证。`check_aircraft_identity_readiness.py` 已收紧为读取可选 `build_status.json` 的 readback/import evidence；`local/carla_air/tmp/aircraft_identity_readiness_after_local_normalized_readback_guard_20260529.json` 仍 `ok=false`、`benchmark_ready=false`，normalized inventory 变为 `identity_count=1` 但 `technical_ready_count=0`、`local_poc_eligible_count=0`、`benchmark_eligible_count=0`。该 POC 是可审计资产规范化尝试，不是 benchmark identity，不关闭 multi-identity blocker。

latest_current_followup_readiness: 2026-05-29 继续按当前 worktree 做聚焦复核并记录 `research/reports/carla_air_current_followup_readiness_2026_05_29_zh.md`。本轮未新增 subagent，因为前一轮已完成 research/docs 与 `mvp-demo` / `tools/carla_air` / outputs 双侧只读 explorer 核查，本轮只需少量 gate 重跑和工具链探针。fresh outputs 为 `local/carla_air/tmp/*_current_followup_20260529.json`；`goal_execution_matrix_current_followup_20260529.json` 仍 `ok=true`、`missing_or_unreadable=[]`、`can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`。Meshlab 新探针显示 raw FBX `dji_drone_fbx_obj/extracted/modelNew.fbx` 与 `dji_drone_fbx_abc/.../Drone_fb.FBX` 可读取并导出 OBJ，但 raw OBJ importer 会 abort，Meshlab FBX re-export/roundtrip 不能保存可回读 FBX；本机仍缺 Blender/assimp/fbx2gltf/UE/bpy。该结果只增加 raw-source inspect evidence，不能把 `dji_drone_fbx_obj_local_poc` 或任何 raw asset 升级为 benchmark identity；`aircraft_identity_readiness_current_followup_20260529.json` 仍 `benchmark_ready=false`、`technical_ready_count=0`、`benchmark_eligible_count=0`。本轮未启动 simulator、未执行 formal conversion / NeoVerse / benchmark 写入，正式三条路线仍 blocked。

latest_session_reaudit: 2026-05-29 按当前 worktree 重新取证并记录 `research/reports/carla_air_session_reaudit_2026_05_29_zh.md`。本轮按仓库级策略并行调用 research/docs 侧与 `mvp-demo` / `tools/carla_air` / runtime outputs 侧只读 explorer；主 agent 复核后以 fresh JSON 为准裁决早期报告冲突。输出 `local/carla_air/tmp/poc03_target_selection_readiness_session_reaudit_20260529.json` 仍 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`；`neoverse_geometry_readiness_session_reaudit_20260529.json` 仍 `formal_neoverse_ready=false`、`depth_backprojection_is_neoverse_reconstruction=false`、`formal_neoverse_output_count=0`；`goal_execution_matrix_session_reaudit_20260529.json` 仍 `can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`。`poc03_synthetic_verification_session_reaudit_20260529.json` 为 `ok=true`、`failure_count=0`，只支持 validated synthetic depth-backprojection smoke，不是 pixel-accurate identity proof、NeoVerse reconstruction 或 final real 4D geometry。multi-identity 当前更新为 3 个 local technical POC：`aircraft_identity_readiness_session_reaudit_20260529.json` 显示 `normalized_identity_count=3`、`technical_ready_count=3`、`benchmark_eligible_count=0`；`aircraft_identity_roster_plan_session_reaudit_20260529.json` 显示 `local_poc_multi_identity_ready=true`、`formal_benchmark_ready=false`、`benchmark_roster_count=0`；`aircraft_identity_import_smoke_session_reaudit_20260529.json` 显示 3 个 local POC Assimp import smoke 均通过，但这不是 UE/CARLA import 或 benchmark permission。最终 runtime 快照只见 CARLA `:2000` listening，未见 AirSim `:41451`，因此本轮未执行 AirSim-dependent live export/capture/inspect。

latest_identity_permission_evidence_guard: 2026-05-29 新增只读 `tools/carla_air/audit_aircraft_identity_permission_evidence.py`，并收紧 `tools/carla_air/check_aircraft_identity_readiness.py` 的 formal benchmark eligibility：除 metadata permission flags 外，还必须存在带 locator 与 quote/summary 的 license evidence 以及 benchmark/public/redistribution permission evidence。报告为 `research/reports/carla_air_identity_permission_evidence_guard_2026_05_29_zh.md`；输出 `local/carla_air/tmp/aircraft_identity_permission_evidence_audit_20260529.json` 为 `ok=false`、`identity_count=3`、`benchmark_permission_ready_count=0`。重跑 `local/carla_air/tmp/aircraft_identity_readiness_permission_evidence_guard_20260529.json` 后仍 `normalized_identity_count=3`、`technical_ready_count=3`、`local_poc_eligible_count=3`、`benchmark_eligible_count=0`，且每个 local POC 额外带有 `asset_meta_license_evidence_present` 与 `asset_meta_benchmark_permission_evidence_present` blockers。`aircraft_identity_roster_plan_permission_evidence_guard_20260529.json` 仍 `local_poc_multi_identity_ready=true`、`formal_benchmark_ready=false`、`benchmark_roster_count=0`；Assimp import smoke 仍通过但 `not_license_or_benchmark_permission=true`。该 guard 不修改 asset metadata，不授予许可，不把 Aigei promotional notes 当作 benchmark permission，不关闭 multi-identity benchmark blocker。

latest_identity_ue_carla_import_readiness_guard: 2026-05-29 新增只读 `tools/carla_air/audit_aircraft_identity_ue_carla_import_readiness.py`，并进一步收紧 `tools/carla_air/check_aircraft_identity_readiness.py` / `tools/carla_air/plan_aircraft_identity_roster.py`：formal benchmark identity 除 technical normalized asset 与 source/license/permission evidence 外，还必须有 UE/CARLA import evidence。报告为 `research/reports/carla_air_identity_ue_carla_import_readiness_2026_05_29_zh.md`；输出 `local/carla_air/tmp/aircraft_identity_ue_carla_import_readiness_20260529.json` 为 `ok=false`、`identity_count=3`、`verified_identity_count=0`、`can_run_ue_import_smoke_now=false`，blockers 为 `unreal_editor_missing` 与 `identity_ue_carla_import_evidence_missing`。packaged `CarlaUE4.uproject` 与 `CarlaUE4-Linux-Shipping` 存在，但本机没有 `UE4Editor` / `UnrealEditor` 命令，shipping runtime 不作为 editor import toolchain。重跑 `local/carla_air/tmp/aircraft_identity_readiness_ue_carla_import_guard_20260529.json` 后仍 `technical_ready_count=3`、`local_poc_eligible_count=3`、`benchmark_eligible_count=0`，每个 local POC 新增 `build_status_ue_carla_import_verified` blocker；`local/carla_air/tmp/goal_execution_matrix_ue_carla_import_guard_20260529.json` 仍显示三条正式路线不可执行。

latest_formal_target_selection_guard: 2026-05-29 按当前 worktree 重新取证后，收紧正式 synthetic annotation / geometry 写入入口并记录 `research/reports/carla_air_formal_target_selection_guard_2026_05_29_zh.md`。本轮按仓库级策略并行调用 research/docs 侧与 implementation/output 侧只读 explorer；两者均确认现有输出仍不能证明 real synthetic annotation、true 4D geometry 或 formal multi-identity benchmark。`tools/carla_air/convert_synthetic_sensor_masks.py` 与 `tools/carla_air/backproject_synthetic_depth_points.py` 现在在 formal path 中都要求 `--target-selection-readiness`，且 report 与 candidate assessment 必须同时证明 `target_selection_ready`、`trusted_target_id_or_tag_available`、`ready_for_formal_conversion`、`identity_proof`、`pixel_accurate` 与 `formal_synthetic_annotation_ready`；`tools/carla_air/run_poc03_synthetic_reid_pipeline.py` 新增 passthrough，并在 execute 到 convert 及之后时缺 readiness 直接拒绝。新输出 `local/carla_air/tmp/poc03_plan_formal_guard_no_readiness_20260529.json` 显示 formal convert/points 缺 readiness blocker；`local/carla_air/tmp/poc03_target_selection_readiness_formal_guard_plan_20260529.json` 仍 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`、`identity_proof=false`、`pixel_accurate=false`；`local/carla_air/tmp/goal_execution_matrix_formal_guard_plan_20260529.json` 仍 `can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`。负向 smoke 证明 convert / points / execute orchestrator 均会在写入前拒绝 candidate-only evidence，且 node03 official scene contract、annotation、tracklets 与 points meta sha256 未变化。本轮仍没有完成正式 annotation / true 4D geometry / formal benchmark。

latest_synthetic_verifier_target_selection_guard: 2026-05-29 继续收紧 post-run verifier 并记录 `research/reports/carla_air_synthetic_verifier_target_selection_guard_2026_05_29_zh.md`。`tools/carla_air/verify_poc03_synthetic_reid_outputs.py` 现在要求 track / annotation meta / points meta 中的 `target_validation.target_selection_readiness` 存在，并且 report 级与 candidate assessment 均证明 trusted actor-to-pixel target、identity proof、pixel accuracy 与 formal synthetic annotation readiness。旧 POC03 synthetic smoke 负向验证输出 `local/carla_air/tmp/poc03_synthetic_verification_target_selection_guard_negative_20260529.json` 与 `..._negative_exit_20260529.json`：`ok=false`、`failure_count=84`，84 个 failure 全部为 `target_selection_readiness*`；默认不带 `--allow-fail` 的 verifier 返回 `RC=1`。因此旧 `target_candidate_gate_passed_not_identity_proof` lineage 不再能被 verifier 误判为 formal synthetic output pass；这些旧输出只能保留为历史 smoke / regression artifact，不是正式 annotation、pixel-accurate mask、final geometry 或 benchmark evidence。

latest_scene_status_target_selection_guard: 2026-05-29 继续收紧 scene-level 状态审计并记录 `research/reports/carla_air_scene_status_target_selection_guard_2026_05_29_zh.md`。`tools/carla_air/audit_scene_pipeline_status.py` 现在要求 synthetic annotation 与 synthetic depth-backprojection variant 同时具备 target-selection readiness evidence，旧 POC03 candidate-only synthetic smoke 不再计为 `synthetic_annotation_and_depth_geometry_ready`。最新输出 `local/carla_air/tmp/scene_pipeline_status_audit_target_selection_guard_20260529.json` / `.csv` 显示 7 个官方 scene 中 5 个为 `skeleton_pending_formal_annotations`，2 个 POC03 scene 为 `synthetic_candidate_smoke_needs_target_selection`；`synthetic_annotation_ready_count=0`、`synthetic_depth_points_ready_count=0`、`synthetic_candidate_smoke_count=2`、`synthetic_target_selection_ready_count=0`。CSV 也新增 candidate smoke / target-selection ready 列，避免把 historical smoke 误读为 formal ready。本轮仍没有完成正式 annotation、true 4D geometry 或 formal benchmark。

latest_target_selection_controlled_decode_guard: 2026-05-29 继续推进 POC03 target-selection readiness gate，并记录 `research/reports/carla_air_target_selection_controlled_decode_guard_2026_05_29_zh.md`。`tools/carla_air/audit_poc03_target_selection_readiness.py` 现在读取受控 actor instance-id calibration、close-view alternate decode probe，并实时探测 CARLA/AirSim 端口。最新输出 `local/carla_air/tmp/poc03_target_selection_readiness_controlled_decode_guard_20260529.json` 为 `ok=true` 但 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`；结构化证据显示 ordinary controlled actor `345` 可由 `green_plus_256_blue` 解码命中 9532 个 ROI 像素，但 POC03 actor `24` 在同一 close-view alt decode 中 `actor_id_seen_in_close_roi=false` / `actor_id_pixel_count=0`。当前实时端口为 CARLA `2000` open、AirSim `41451` closed，因此不能刷新 AirSim-dependent live target evidence。`local/carla_air/tmp/goal_execution_matrix_controlled_decode_guard_20260529.json` 仍显示 formal conversion、NeoVerse、formal multi-identity roster 三条路线均不可执行。

latest_mask_tracklet_lineage_required_guard: 2026-05-29 在 `research/reports/carla_air_mask_tracklet_embedding_provenance_2026_05_29_zh.md` 的基础上，继续记录 `research/reports/carla_air_mask_tracklet_lineage_required_guard_2026_05_29_zh.md`。本轮按仓库级策略并行调用 research/docs 侧与 `mvp-demo` / `tools/carla_air` / outputs 侧只读 explorer；主 agent 裁决以 target-selection guard 后的 `local/carla_air/tmp/scene_pipeline_status_audit_target_selection_guard_20260529.json` 为当前语义，早期 `synthetic_*_ready_count=2` 只能作为 historical smoke 快照。`mvp-demo/scripts/extract_track_embeddings.py` 新增 `--require-tracklet-lineage`：打开后要求 `mask_tracklet_v2.input_lineage` 存在，并逐帧核对当前 RGB frame sha256、mask sha256 与 bbox；默认模式仍兼容 legacy tracklet。正例 `local/carla_air/tmp/mask_tracklet_provenance_smoke_20260529/embeddings_lineage_required/` 输出 shape `(1, 161)`、finite、3 帧均 `tracklet_lineage_verified=true`；负例 `local/carla_air/tmp/mask_tracklet_provenance_negative_20260529/` 修改 1 个 mask 像素后以 `RC=1` 拒绝，错误为 `tracklet lineage mask_sha256 mismatch`。`local/carla_air/tmp/goal_execution_matrix_mask_tracklet_lineage_required_20260529.json` 仍显示 `can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`。该 guard 只防 stale mask-derived 下游消费，不是 formal annotation、identity proof、pixel-accurate mask、true 4D geometry 或 benchmark evidence。

latest_semantic_lidar_scene_lineage_guard: 2026-05-29 继续收紧 semantic-lidar actor points candidate verifier，并记录 `research/reports/carla_air_semantic_lidar_scene_lineage_guard_2026_05_29_zh.md`。本轮未新增 subagent，因为读取当前计划、handoff 与 verifier/exporter 后，任务收敛为单个 verifier 的局部 guard。`tools/carla_air/verify_semantic_lidar_actor_points_candidate.py` 现在反查 `meta.scene_dir` 下的 `capture_meta.json` 与 `trajectory_frame_groups.csv`，要求 scene/node/identity/timestamp/node_dir、observation planned frame 与由 recorded `drone_carla_pose_json + sensor_placement.offset_xyz_m` 复算的 `sensor_pose` 全部一致；report `ok=false` 时默认 `RC=1`，`--allow-fail` 可只写报告。正例 `local/carla_air/tmp/semantic_lidar_actor_points_candidate_verification_scene_lineage_exit_20260529.json` 为 `ok=true`、`failure_count=0`、`point_root_count=2`，新增 lineage checks 覆盖 20 个 timestamp；负例 `local/carla_air/tmp/semantic_lidar_actor_points_lineage_negative_20260529/report_exit.json` 篡改 index timestamp 后为 `ok=false`、`failure_count=5` 且默认 `RC=1`。`local/carla_air/tmp/goal_execution_matrix_semantic_lidar_scene_lineage_guard_20260529.json` 仍显示正式 conversion、NeoVerse、multi-identity 三条路线不可执行。该 guard 只证明 actor-relative semantic-lidar candidate root 与 scene lineage 自洽，不是 camera pixel mask、fixed-camera capture geometry、NeoVerse reconstruction、final real 4D geometry 或 benchmark evidence。

latest_semantic_lidar_actor_local_geometry_candidate: 2026-05-29 新增离线 actor-local geometry candidate 派生与 verifier，并记录 `research/reports/carla_air_semantic_lidar_actor_local_geometry_candidate_2026_05_29_zh.md`。`tools/carla_air/derive_semantic_lidar_actor_local_geometry.py` 消费已通过 scene-lineage verifier 的 `carla_semantic_lidar_actor_idx_v1` roots，把 world XYZ 转成每 timestamp 的 `actor_local_xyz`，输出 `local/carla_air/tmp/semantic_lidar_actor_local_geometry_node03_limit10_20260529/points_by_timestamp/` 与 `...node04.../points_by_timestamp/`；derivation report `local/carla_air/tmp/semantic_lidar_actor_local_geometry_derivation_20260529.json` 为 `ok=true`、`failure_count=0`、`root_count=2`，node03 为 10 timestamps / 54474 points，node04 为 10 timestamps / 54094 points，roundtrip max error 均为 `0.0m`。`tools/carla_air/verify_semantic_lidar_actor_local_geometry.py` 输出 `local/carla_air/tmp/semantic_lidar_actor_local_geometry_verification_20260529.json` 为 `ok=true`、`failure_count=0`、`root_count=2`、`final_real_4d_geometry=false`；`local/carla_air/tmp/goal_execution_matrix_after_actor_local_geometry_20260529.json` 仍 `can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`。该结果是更强 actor-level geometry consistency candidate，但仍 `diagnostic_only=true`、`fixed_camera_capture_geometry=false`、`neoverse_reconstruction=false`、`final_real_4d_geometry=false`，不写 scene outputs、不更新 contract、不替换 official `points_by_timestamp`。

latest_multi_identity_local_poc_current_gate: 2026-05-29 按当前 worktree 重新核对 multi-identity gate，并记录 `research/reports/carla_air_multi_identity_local_poc_current_gate_2026_05_29_zh.md`。本轮按仓库级策略并行调用 research/docs 侧与 `mvp-demo` / `tools/carla_air` / outputs 侧只读 explorer；两者均确认当前 annotation / geometry / benchmark 仍未正式完成。fresh outputs 显示 `local/carla_air/tmp/aircraft_identity_readiness_current_multi_local_poc_20260529.json` 为 `ok=false`、`benchmark_ready=false`，但 `normalized_inventory.identity_count=3`、`technical_ready_count=3`、`local_poc_eligible_count=3`、`benchmark_eligible_count=0`；`local/carla_air/tmp/aircraft_identity_roster_plan_current_multi_local_poc_20260529.json` 为 `local_poc_multi_identity_ready=true`、`formal_benchmark_ready=false`、`local_poc_roster_count=3`、`benchmark_roster_count=0`；`local/carla_air/tmp/aircraft_identity_import_smoke_current_multi_local_poc_20260529.json` 为 `ok=true`、`passed_identity_count=3`，但 `not_ue_or_carla_import=true` 且 `not_license_or_benchmark_permission=true`；`local/carla_air/tmp/aircraft_identity_ue_carla_import_readiness_current_multi_local_poc_20260529.json` 为 `ok=false`、`verified_identity_count=0`、`can_run_ue_import_smoke_now=false`，blockers 为 `unreal_editor_missing` 与 `identity_ue_carla_import_evidence_missing`。`local/carla_air/tmp/goal_execution_matrix_current_multi_local_poc_20260529.json` 仍 `can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`。当前可称为 3-identity local technical POC gate pass，但不能称为 formal multi-identity benchmark；runtime snapshot 只见 CARLA `:2000` open，AirSim `:41451` closed。

latest_local_poc_multi_identity_guard: 2026-05-29 新增只读 `tools/carla_air/verify_local_poc_multi_identity_guard.py`，并记录 `research/reports/carla_air_local_poc_multi_identity_guard_2026_05_29_zh.md`。该 guard 交叉核查 readiness、roster、Assimp import smoke、permission evidence、UE/CARLA import readiness 与 goal matrix。正例 `local/carla_air/tmp/local_poc_multi_identity_guard_20260529.json` 为 `ok=true`、`local_poc_multi_identity_ready=true`、`formal_benchmark_ready=false`、`goal_complete=false`、`local_poc_roster_count=3`、`benchmark_roster_count=0`、`permission_ready_count=0`、`ue_carla_verified_identity_count=0`，并确认 `can_plan_multi_identity_roster_now=false`。负例 `local/carla_air/tmp/local_poc_multi_identity_guard_negative_20260529/report.json` 篡改 import smoke identity 后默认 `RC=1`、`ok=false`、`failure_count=3`，关键 failure 为 `import_smoke_identity_set_mismatch`、`normalized_fbx_sha256_mismatch`、`identity_import_geometry_empty`。该 guard 只证明 3-identity local technical POC roster 自洽，不是 formal benchmark verifier。

latest_private_model_policy_split: 2026-05-30 按用户确认的自用/未开源策略，将 `assets/models/` 下载/购买模型的许可 gate 拆成 private-local 与 public/formal 两层，并记录 `research/reports/carla_air_private_model_policy_split_2026_05_30_zh.md`。`assets/models/aircraft_normalized/dji_drone_fbx_obj_local_poc/asset_meta.json`、`f22_fbx_obj_local_poc/asset_meta.json`、`j20_fbx_obj_local_poc/asset_meta.json` 现在显式记录 `private_local_use_allowed=true`、`private_benchmark_allowed=true`、`public_benchmark_eligible=false`、`open_source_release_blocker=true`，但继续保持 `benchmark_allowed=false`、`public_dataset_allowed=false`、`redistribution_allowed=false`。`tools/carla_air/check_aircraft_identity_readiness.py` / `plan_aircraft_identity_roster.py` 已区分 `private_benchmark_eligible` 与严格公开/正式 `benchmark_eligible`；`tools/carla_air/build_local_normalized_aircraft_identity.py` 也会给后续生成的 normalized asset 写入同样 private-use metadata。主审输出 `local/carla_air/tmp/aircraft_identity_readiness_private_policy_final_20260530.json` 为 `private_benchmark_eligible_count=6`、`private_carla_benchmark_eligible_count=0`、`benchmark_eligible_count=0`；`local/carla_air/tmp/aircraft_identity_roster_plan_private_policy_final_20260530.json` 为 `private_benchmark_multi_identity_ready=true`、`private_benchmark_roster_count=6`、`formal_benchmark_ready=false`、`benchmark_roster_count=0`；`local/carla_air/tmp/goal_execution_matrix_private_policy_final_20260530.json` 明确 `private_benchmark_can_progress_without_public_license_evidence=true`，但 `can_plan_multi_identity_roster_now=false`。结论：版权/公开再分发证据不再阻塞当前私有本地 benchmark 推进；开源发布、公开数据集、可再分发 benchmark 和 formal public benchmark 仍必须补齐 source/license/redistribution/UE-CARLA import evidence。

latest_local_annotation_hook_surface_audit: 2026-05-29 新增只读 `tools/carla_air/audit_local_annotation_hook_surface.py`，并记录 `research/reports/carla_air_local_annotation_hook_surface_audit_2026_05_29_zh.md`。`local/carla_air/tmp/local_annotation_hook_surface_audit_20260529.json` 为 `ok=true`、`failure_count=0`、`scanned_files=65`、`finding_count=198`、`read_surface=191`、`config_render_support=7`、`setter_or_assignment_candidate=0`、`local_hook_found=false`、`actionable_hook_found=false`、`strong_setter_evidence=false`。这次离线扫描只确认文本/config/source 里有读侧/渲染支持面，没有可直接用于 actor-level instance/segmentation/stencil 赋值或查询的本地 hook，也没有改变任何 formal gate。

latest_procedural_identity_permission_poc: 2026-05-29 新增 project-owned procedural identity `procedural_delta_uav_v1` 并记录 `research/reports/carla_air_procedural_identity_permission_poc_2026_05_29_zh.md`。raw source 与 license evidence 位于 `assets/models/aircraft_raw/procedural_delta_uav_v1/`，normalized outputs 位于 `assets/models/aircraft_normalized/procedural_delta_uav_v1/`。fresh outputs 显示 `aircraft_identity_readiness_procedural_permission_poc_20260529.json` 为 `identity_count=4`、`technical_ready_count=4`、`local_poc_eligible_count=4`、`benchmark_eligible_count=0`；`aircraft_identity_permission_evidence_procedural_permission_poc_20260529.json` 为 `benchmark_permission_ready_count=1`，只有 procedural identity permission-ready；`aircraft_identity_import_smoke_procedural_permission_poc_20260529.json` 为 `ok=true`、`passed_identity_count=4`；`aircraft_identity_ue_carla_import_readiness_procedural_permission_poc_20260529.json` 仍 `ok=false`、`verified_identity_count=0`、`can_run_ue_import_smoke_now=false`；`local_poc_multi_identity_guard_procedural_permission_poc_20260529.json` 为 `ok=true`、`local_poc_multi_identity_ready=true`、`formal_benchmark_ready=false`。`tools/carla_air/verify_local_poc_multi_identity_guard.py` 已同步允许 partial permission improvement，但仍要求 permission incomplete、UE/CARLA import missing 与 goal matrix blocked。该 POC 只把 local technical roster 推进到 4 identities，不是 formal benchmark readiness。

latest_procedural_twinboom_identity_poc: 2026-05-29 新增第二个 project-owned procedural identity `procedural_twinboom_uav_v1` 并记录 `research/reports/carla_air_procedural_twinboom_identity_poc_2026_05_29_zh.md`。raw source 与 license evidence 位于 `assets/models/aircraft_raw/procedural_twinboom_uav_v1/`，normalized outputs 位于 `assets/models/aircraft_normalized/procedural_twinboom_uav_v1/`。fresh outputs 显示 `aircraft_identity_readiness_procedural_twinboom_poc_20260529.json` 为 `identity_count=5`、`technical_ready_count=5`、`local_poc_eligible_count=5`、`benchmark_eligible_count=0`；`aircraft_identity_permission_evidence_procedural_twinboom_poc_20260529.json` 为 `benchmark_permission_ready_count=2`；`aircraft_identity_import_smoke_procedural_twinboom_poc_20260529.json` 为 `ok=true`、`passed_identity_count=5`；`aircraft_identity_ue_carla_import_readiness_procedural_twinboom_poc_20260529.json` 仍 `ok=false`、`verified_identity_count=0`、`can_run_ue_import_smoke_now=false`；`local_poc_multi_identity_guard_procedural_twinboom_poc_20260529.json` 为 `ok=true`、`local_poc_multi_identity_ready=true`、`formal_benchmark_ready=false`。`tools/carla_air/verify_local_poc_multi_identity_guard.py` 已同步允许 permission audit 达到局部 threshold，但仍要求 full-roster permission incomplete、UE/CARLA import missing 与 goal matrix blocked。该 POC 只把 local technical roster 推进到 5 identities、permission-ready identities 推进到 2，不是 formal benchmark readiness。

latest_procedural_canard_identity_poc: 2026-05-29 新增第三个 project-owned procedural identity `procedural_canard_uav_v1` 并记录 `research/reports/carla_air_procedural_canard_identity_poc_2026_05_29_zh.md`。raw source 与 license evidence 位于 `assets/models/aircraft_raw/procedural_canard_uav_v1/`，normalized outputs 位于 `assets/models/aircraft_normalized/procedural_canard_uav_v1/`。fresh outputs 显示 `aircraft_identity_readiness_procedural_canard_poc_20260529.json` 为 `identity_count=6`、`technical_ready_count=6`、`local_poc_eligible_count=6`、`benchmark_eligible_count=0`；`aircraft_identity_permission_evidence_procedural_canard_poc_20260529.json` 为 `benchmark_permission_ready_count=3`；`aircraft_identity_import_smoke_procedural_canard_poc_20260529.json` 为 `ok=true`、`passed_identity_count=6`；`aircraft_identity_ue_carla_import_readiness_procedural_canard_poc_20260529.json` 仍 `ok=false`、`verified_identity_count=0`、`can_run_ue_import_smoke_now=false`；`local_poc_multi_identity_guard_procedural_canard_poc_20260529.json` 为 `ok=true`、`local_poc_multi_identity_ready=true`、`formal_benchmark_ready=false`。该 POC 只把 local technical roster 推进到 6 identities、permission-ready identities 推进到 3，不是 formal benchmark readiness。

latest_identity_ue_import_smoke_plan: 2026-05-29 新增只读 UE/CARLA import smoke runbook generator / verifier，并记录 `research/reports/carla_air_identity_ue_import_smoke_plan_2026_05_29_zh.md`。`tools/carla_air/plan_aircraft_identity_ue_import_smoke.py` 输出 `local/carla_air/tmp/aircraft_identity_ue_import_smoke_plan_procedural_canard_poc_20260529.json`，`identity_count=6`、`planned_identity_count=6`、`runnable_now_count=0`、`blocked_now_count=6`、`already_verified_count=0`、`formal_benchmark_ready=false`、`goal_complete=false`，blocker 是没有 `UE4Editor` / `UnrealEditor` import toolchain；packaged `CarlaUE4.uproject` 与 shipping runtime 存在但 shipping runtime 不是 editor import toolchain。`tools/carla_air/verify_aircraft_identity_ue_import_smoke_plan.py` 输出 `local/carla_air/tmp/aircraft_identity_ue_import_smoke_plan_verification_procedural_canard_poc_20260529.json`，`ok=true`、`failure_count=0`、`verified_identity_plan_count=6`、`non_promotion_verified=true`。该 plan/verifier 只定义未来成功 import smoke 后可写入的 per-identity evidence 字段，不运行 Unreal、不写 asset metadata、不 patch `build_status.json`、不把任何 identity 升级为 benchmark-eligible。

latest_ue_import_smoke_plan_status_sync: 2026-05-29 将 UE/CARLA import smoke plan 同步到默认状态入口、ACTIVE_PLAN 与 handoff，并记录 `research/reports/carla_air_ue_import_smoke_plan_status_sync_2026_05_29_zh.md`。本轮两个只读 explorer 与主 agent 本地核对一致：formal annotation、NeoVerse/final geometry、formal multi-identity benchmark 仍 blocked。复跑 plan 输出 `local/carla_air/tmp/aircraft_identity_ue_import_smoke_plan_recheck_20260529.json` 为 `ok=false`、`identity_count=6`、`runnable_now_count=0`、`blocked_now_count=6`、`formal_benchmark_ready=false`；复跑 verifier 输出 `local/carla_air/tmp/aircraft_identity_ue_import_smoke_plan_verification_recheck_20260529.json` 为 `ok=true`、`failure_count=0`、`verified_identity_plan_count=6`、`non_promotion_verified=true`。`ok=false` 的原因是预期中的 `UE4Editor` / `UnrealEditor` 缺失，不是 plan schema 或 non-promotion failure。

latest_research_docs_soft_archive: 2026-05-29 新增默认当前状态入口 `research/CURRENT_STATUS.md`，并记录 `research/reports/carla_air_research_docs_soft_archive_2026_05_29_zh.md`。本轮未移动旧报告，只把 `ACTIVE_PLAN.md` 的 `must_read` 从长历史列表收敛为当前入口、handoff 与少数关键 gate 报告；旧 `research/reports/*.md` 保留原路径作为 provenance / regression history，只有追溯具体 artifact、命令、输出路径、sha256 lineage 或 blocker 时按需读取。该整理不改变技术 gate：formal annotation、formal NeoVerse/final geometry 与 formal multi-identity benchmark 仍 blocked。

latest_ue_import_smoke_evidence_applier: 2026-05-29 新增 `tools/carla_air/apply_ue_import_smoke_evidence.py` 与对应报告 `research/reports/carla_air_ue_import_smoke_evidence_applier_2026_05_29_zh.md`。该 applier 只做 UE import smoke evidence 的受控应用与只读/乾运行核对，不加入默认 must_read，也不改变当前 gate 状态：`normalized_identity_count=6`、`benchmark_permission_ready_count=3`、`ue_carla_verified_identity_count=0`、`benchmark_eligible_count=0`，annotation / proxy / final geometry 仍 unchanged。验证结论保持：readonly positive fixture `ok=true`、`patch_applied=false`；dry-run apply negative `ok=false`、`patch_applied=false`；`research/` report negative 拒绝 `report_under_research_forbidden` / `report_outside_local_forbidden`。

latest_proxy_replacement_offline_regression_gate: 2026-05-29 新增只读 `tools/carla_air/verify_proxy_replacement_offline_regression.py` 与报告 `research/reports/carla_air_proxy_replacement_offline_regression_gate_2026_05_29_zh.md`。该 verifier 聚合 target traceability、final geometry gate 与 goal execution matrix，默认只验证当前 blocked invariant，不写 scene/assets/contracts、不启动 runtime。主审输出 `local/carla_air/tmp/proxy_replacement_offline_regression_main_review_20260529.json` 为 `ok=true`、`failure_count=0`、`any_upstream_positive=false`、`expect_blocked=true`；inconsistent true fixture 为 `ok=false`、`failure_count=8`，包含 `blocked_invariant_breached` 与 `positive_dependency_mismatch`；consistent route-positive fixture 在 `--no-expect-blocked` 下为 `ok=true`，证明未来 annotation / geometry route 可在尚未 `goal_complete=true` 时保持一致性通过。该 gate 不替换 proxy annotation / points，不创建 final 4D geometry，不运行 NeoVerse/runtime，不改变 benchmark readiness。
latest_proxy_replacement_contract_integration: 2026-05-29 新增 `tools/carla_air/verify_proxy_replacement_offline_regression.py` 的 contract integration 报告 `research/reports/carla_air_proxy_replacement_contract_integration_2026_05_29_zh.md`。该离线 verifier 现在默认消费 `local/carla_air/tmp/synthetic_annotation_evidence_contract_main_review_blocked_20260529.json` 与 `local/carla_air/tmp/final_geometry_evidence_contract_main_review_blocked_20260529.json`；主审 `local/carla_air/tmp/proxy_replacement_offline_regression_contract_integration_main_review_blocked_20260529.json` 仍 `ok=true`、`failure_count=0`、`expect_blocked=true`、`any_upstream_positive=false`，blockers 仍是 `manifest_unavailable` / `manifest_unavailable`。route-positive fixture 仅证明 `--no-expect-blocked` 下的一致性；该集成不运行 runtime，不替换 annotation / geometry，不写 scene/assets/contracts，也不完成 benchmark。
latest_final_geometry_evidence_contract_verifier: 2026-05-29 新增只读 `tools/carla_air/verify_final_geometry_evidence_contract.py` 与报告 `research/reports/carla_air_final_geometry_evidence_contract_verifier_2026_05_29_zh.md`。该 verifier 只检查未来 final-geometry evidence contract，不运行 NeoVerse/runtime，不创建 final geometry，不替换 proxy points，也不完成 benchmark。`local/carla_air/tmp/final_geometry_evidence_contract_main_review_blocked_20260529.json` 维持 `ok=false`、`formal_final_geometry_ready=false`、`can_replace_proxy_points_now=false`、`goal_complete=false`，blocker 为 `manifest_unavailable`；`local/carla_air/tmp/final_geometry_evidence_contract_positive_fixture_verification_20260529.json` 仅做严格 positive fixture 验证，仍 `goal_complete=false`。diagnostic 与 research-manifest negative verifications 继续拒绝 promotion，当前 final geometry / proxy replacement / benchmark 状态不变。

latest_runtime_guarded_defaults_recheck: 2026-05-29 进一步复核 runtime guarded defaults，并记录 `research/reports/carla_air_runtime_guarded_defaults_recheck_2026_05_29_zh.md`。本轮同步了 `tools/carla_air/audit_goal_execution_matrix.py` 与 `tools/carla_air/audit_candidate_formalization_readiness.py` 的当前 guarded defaults，fresh outputs 为 `local/carla_air/tmp/goal_execution_matrix_runtime_ports_candidate_defaults_final_20260529.json` 与 `local/carla_air/tmp/candidate_formalization_readiness_current_defaults_final_20260529.json`。前者 `ok=true`、`current_runtime_ports` 显示 CARLA `2000` open、AirSim `41451` closed，且 `all_required_ports_open=false`、`can_execute_formal_conversion_now=false`、`can_run_formal_neoverse_now=false`、`can_plan_multi_identity_roster_now=false`；后者 `ok=true`、`formalization_ready=false`、`goal_complete=false`、`official_synthetic_smoke.ready=false`、`failure_count=84`，scene synthetic_annotation_ready_count=0、synthetic_depth_points_ready_count=0、synthetic_candidate_smoke_count=2、synthetic_target_selection_ready_count=0，multi-identity normalized_identity_count=6、technical_ready_count=6、local_poc_eligible_count=6、benchmark_eligible_count=0。该 recheck 只确认当前 guarded defaults 仍然阻断正式 conversion / NeoVerse / multi-identity benchmark，不把 runtime 端口状态或 candidate smoke 误记为 formal readiness。

must_read:
  - docs/goal.md
  - research/CURRENT_STATUS.md
  - research/guides/carla_air_runtime_self_start_zh.md
  - research/handoffs/carla_air_ground_to_air_collection_handoff_zh.md
  - research/reports/carla_air_runtime_guarded_defaults_recheck_2026_05_29_zh.md
  - research/reports/carla_air_multi_identity_local_poc_current_gate_2026_05_29_zh.md
  - research/reports/carla_air_local_poc_multi_identity_guard_2026_05_29_zh.md
  - research/reports/carla_air_procedural_identity_permission_poc_2026_05_29_zh.md
  - research/reports/carla_air_procedural_twinboom_identity_poc_2026_05_29_zh.md
  - research/reports/carla_air_procedural_canard_identity_poc_2026_05_29_zh.md
  - research/reports/carla_air_goal_execution_matrix_current_defaults_2026_05_29_zh.md
  - research/reports/carla_air_scene_status_target_selection_guard_2026_05_29_zh.md
  - research/reports/carla_air_target_selection_controlled_decode_guard_2026_05_29_zh.md
  - research/reports/carla_air_neoverse_geometry_readiness_2026_05_29_zh.md
  - research/reports/carla_air_identity_permission_evidence_guard_2026_05_29_zh.md
  - research/reports/carla_air_identity_ue_carla_import_readiness_2026_05_29_zh.md
  - research/reports/carla_air_identity_ue_import_smoke_plan_2026_05_29_zh.md
  - research/reports/carla_air_ue_import_smoke_plan_status_sync_2026_05_29_zh.md
  - research/reports/carla_air_semantic_lidar_scene_lineage_guard_2026_05_29_zh.md
  - research/reports/carla_air_semantic_lidar_actor_local_geometry_candidate_2026_05_29_zh.md
  - research/reports/carla_air_mask_tracklet_lineage_required_guard_2026_05_29_zh.md
  - research/reports/carla_air_research_docs_soft_archive_2026_05_29_zh.md
  - older reports under research/reports/ are soft-archived; read them only when tracing a specific lineage, command, output path, or blocker.

locked_decisions:
  - 毕设主线暂时简化为已知固定 camera intrinsics + 已知固定 camera pose；真实弱标定和 NeoVerse predicted camera 只作为后续扩展，不作为当前正式采集前置条件。
  - `local/carla_air/camera_nodes/Town10HD_ground_to_air_nodes_v1.json` 是当前 Town10HD 地对空固定节点配置来源；`node01-node05` 是 active capture platform。
  - `tools/carla_air/capture_camera_nodes_smoke.py` 保持为 capture baseline，在冻结阶段不修改。
  - `presence_gate.json`、`review_summary.csv`、`review_overlays/` 都是 smoke / QC 产物，不是正式标注，也不进入 pipeline。
  - 4B 正式数据契约已冻结，capture 侧权威输入为 `capture_meta.json`、`trajectory_capture_manifest.json`、`trajectory_frame_groups.csv`、`nodes/<node_id>/frame_times.csv`、`nodes/<node_id>/calib/rig.json`、`nodes/<node_id>/cams/cam*/frames/*.png`；后续正式字段再扩展 `depth`、`mask`、`bbox`、`object pose`。
  - 默认 AirSim drone trajectory + presence gate 是当前可复现 fallback；只要自定义 aircraft 替换仍受工具链 blocker 约束，就不把模型替换作为采集主线前置条件。
  - `assets/models/aircraft_raw/` 只作为 asset registry 来源；工具链不可用时不伪造 `assets/models/aircraft_normalized/<identity_id>/normalized.fbx`。
  - 轨迹设计采用 coverage-first 策略：沿节点光轴走廊取 2-3 个距离层，但实际 waypoint 可以在这些层附近的小范围内采样，允许折返、短悬停和轻度姿态变化；目标是最大化单次 run 内的 unique visible nodes、passing cams 和连续可见帧数，而不是追求所有节点同一时刻可见。
  - `run_drone_trajectory_smoke.py`、`check_capture_presence_gate.py`、`run_drone_trajectory_smoke_suite.py` 都属于 smoke / orchestration 工具；当前 suite 只应视为自动化入口，不等价于正式长跑定版。
  - `tools/carla_air/export_capture_scene_dirs.py` 是 4B capture 到 node 级 `scene_dir` skeleton 的当前转换入口；它只复制/链接 4B 权威输入，并写 `pipeline_contract.json`、`tracks/tracklets_status.json`、`recon/points_by_timestamp/status.json`。
  - `tools/carla_air/build_minimal_formal_sample.py` 是当前最小正式样本验证入口：只从 recorded drone pose + fixed camera rig + fixed proxy dimensions 生成 formal proxy annotations，不使用 presence gate / review overlay / mp4。
  - `tools/carla_air/probe_annotation_sources.py` 是 annotation source readiness probe；默认离线只检查 API/端口/文档证据，只有显式 `--live-probe` 才做短 runtime sensor smoke。
  - `tools/carla_air/export_live_synthetic_annotations.py` 是 POC03 node scene 的 live synthetic sensor export scaffold；它输出 depth/semantic/instance sensor frames，并在 raw sensor meta/status/contract 中写同一个 `sensor_export_id`，但在 target-instance mask conversion / validation 前不能声称 pixel-accurate target mask 或最终 tracklets ready；re-export 会把既有 derived synthetic variants 标为 pending / not-ready，避免 stale 输出继续被视为有效。2026-05-29 新增 `--start-index` 支持按 `trajectory_frame_groups.csv` 选择后段窗口；`--append-window` 只允许在 `local/` 隔离 scene 副本中补 raw sensor 缺口，拒绝覆盖所选窗口已有 sensor 文件，不允许直接污染官方 scene。
  - `tools/carla_air/inspect_synthetic_sensor_ids.py` 是 live synthetic sensor frames 的只读候选 id/tag 检查入口；它只输出 candidate statistics、raw sensor `sensor_export_evidence` 与逐 timestamp/camera 的 candidate `input_lineage`（source PNG path / sha256 / image shape），不写 `masks_synth`、`tracklets_synth`、annotation status 或 pipeline contract。`--start-index` 与 exporter 使用同一 `trajectory_frame_groups.csv` 窗口基准，再映射到 `frame_times.csv`，避免 raw export / inspect 窗口漂移。2026-05-29 新增显式 `--include-instance-decode-diagnostics`：默认关闭，正式 `candidates.instance` 仍只使用 `contract_red_plus_256_green` / `red + 256 * green`；alternate decode 只写 `instance_decode_diagnostics`，并标记 `diagnostic_only=true`、`updates_formal_candidates=false`、`updates_conversion_contract=false`、`not_identity_proof=true`。负例验证确认 alternate decode value 不会被 formal validation 消费。
  - `tools/carla_air/validate_synthetic_target_candidate.py` 是 candidate inspection 与 mask conversion 之间的只读 target-candidate gate；它核查候选 JSON 的 scene/node/identity/camera/source/threshold、`sensor_export_evidence`、candidate `input_lineage` 与当前 raw synthetic sensor PNG sha256 一致性，输出 `target_candidate_gate_passed_not_identity_proof` 或失败报告，并把通过检查的 `input_lineage` 带入 validation report；但仍不声称 identity proof 或 pixel accuracy。
  - `tools/carla_air/convert_synthetic_sensor_masks.py` 是 live synthetic sensor frames 到 `masks_synth` / `tracklets_synth` 的承接入口；非 dry-run 转换必须显式给出 target `instance_id` 或 `semantic_tag` 以及通过的 `--target-validation-report`，并把该 evidence 写入 `tracklets_synth` / `annotation_meta_synth`；formal conversion 必须匹配当前 raw sensor `sensor_export_id` 并使用 frozen output namespace；conversion 前还要校验 validation report 内 candidate `input_lineage` 的 path/stem/camera/source/sha256 仍匹配当前 scene raw PNG；输出必须记录每个 timestamp/camera 的 raw source sensor、RGB frame、mask、depth path 与 sha256 lineage；`--allow-unvalidated-target` 只允许 isolated debug 输出，不得作为正式 synthetic 输出。`--start-index` 与 exporter 使用同一 `trajectory_frame_groups.csv` 窗口基准，但 formal conversion 仍必须经 target validation 和主 agent 审核。
  - target-candidate validation 不等价于 identity proof 或 pixel accuracy；`convert_synthetic_sensor_masks.py --mark-pixel-accurate` 必须另给 `--pixel-accuracy-evidence`，否则拒绝执行。
  - `tools/carla_air/backproject_synthetic_depth_points.py` 是 `depth_synth + masks_synth + fixed rig` 到 `neoverse_points_by_timestamp_v1` 的非 proxy geometry baseline；source 写为 `carla_depth_synth_mask_backprojection_v1`，不声称 NeoVerse reconstruction；非 dry-run geometry export 必须携带同一个 `--target-validation-report`，核对 `annotation_meta_synth` 中的 target-validation evidence、validation report 内 candidate `input_lineage` 当前 raw PNG sha256、当前 raw sensor `sensor_export_id`、已发布 synthetic annotation variant，并用 `--update-tracklets` 让下游 embedding 消费新 points；formal export 的 timestamp 来源固定为 `tracks/tracklets_synth.json`，逐项核对 `timestamp_stems`、`mask_paths`、`depth_paths`、mask/depth sha256 与 `frame_times.csv`，避免按全量 frame_times 生成 tracklet 外 stale points；points `meta.json` 必须记录每个 timestamp 的 depth/mask input sha256 与输出 point file sha256。
  - `tools/carla_air/verify_poc03_synthetic_reid_outputs.py` 是 POC03 synthetic run 的 post-run anti-proxy verifier；只读检查 raw synthetic sensor provenance、`tracklets_synth`、`annotation_meta_synth`、`masks_synth`、depth-backproject points、embeddings 与双向 eval，拒绝 proxy source、`masks_gt`、`neoverse_fused` proxy root 和 scene 内 QC artifacts，并要求 track/meta/points meta 都携带通过的 target-validation evidence、可读 report path、匹配的 `sensor_export_id` 以及可核验的 candidate `input_lineage`；同时逐 timestamp 核对 target-validation raw PNG、raw source sensor、mask、depth、points 的 path 与 sha256，拒绝同路径 in-place 替换导致的 stale candidate / derived outputs；也核对 `track.timestamp_stems`、`fused_points_paths`、`points_root/index.csv`、`meta.json.count` 与 points root 下 `.npy` stem，防止 stale / extra point files 满足数量检查；embedding meta 必须使用 `node_track_embedding_meta_v2`，记录当前 `tracklets_synth` hash 与每个 used timestamp 的 frame/mask/points hash，eval JSON 必须使用 `node_track_retrieval_eval_v2` 并绑定当前 query/gallery embedding 文件 hash，防止 stale embedding/eval JSON 通过存在性与 metric-key 检查。
  - `tools/carla_air/run_poc03_synthetic_reid_pipeline.py` 是 POC03 node03/node04 synthetic sensor -> candidate id inspection -> target-candidate validation -> target mask -> depth-backprojection points -> embeddings -> eval -> verify 的受保护编排入口；默认只生成 plan，`--execute` 运行到 export 阶段必须 runtime 在线，进入 conversion 阶段必须显式给出 target `instance_id` 或 `semantic_tag` 且不允许跳过 target validation；默认 `--stop-after=verify`，早停到 `eval` 只作为显式诊断模式；plan-only 输出已包含 `readiness` 区块，用于显式报告 CARLA/AirSim runtime blocker、target id/tag 是否已选择、当前 synthetic 输出是否存在、下一条 inspect 执行命令、inspect 后 verify 执行模板和必须进入 target validation 的规则。plan-only 允许用 `<instance_id>` / `<semantic_tag>` 占位展示完整链路，含占位符的 step 会标记 `template_requires_user_values=true` / `template_placeholders=[...]`，并在顶层 `readiness.templated_steps_require_user_values` / `readiness.templated_steps` 汇总；执行模式仍强制真实 target 且拒绝含占位符计划。`--append-window` 只用于 raw sensor gap filling / inspect / validation 诊断，plan 会阻止其进入 formal convert / points / eval / verify，并阻止在非 `local/` scene 上执行。
  - `tools/carla_air/audit_scene_pipeline_status.py` 是官方 exported scene 的只读状态审计入口；默认扫描 `mvp-demo/data/carla_air/nodes`，输出 formal inputs / proxy annotation / proxy points / synthetic annotation / synthetic geometry / embeddings / eval refs 的状态矩阵；它只检查 scene 内 QC artifacts 是否存在，不把 presence gate、review 或 mp4 当 formal inputs。2026-05-29 新增 candidate artifact guard，会报告 `masks_actor_bbox*`、`tracklets_actor_bbox*`、`embeddings_actor_bbox*` 等候选/调试产物，并显式标注 checked but not consumed as formal inputs，不改变 scene status。
  - `tools/carla_air/audit_synthetic_target_mask_quality.py` 是 POC03 synthetic target mask 的只读质量诊断入口；输出 large-mask / edge-touch / near-full-width / proxy-overlap 等风险信号，不修改 scene state，也不构成 pixel-accuracy proof、identity proof 或 real geometry evidence。
  - `tools/carla_air/probe_target_actor_binding.py` 是 live target actor / pose / segmentation binding 诊断入口；它可临时 replay 记录行并恢复 runtime pose，只写 `local/carla_air/tmp/` JSON，不修改 scene_dir。当前 v4 证据支持 recorded drone pose 绑定到 CARLA `airsim.drone` actor / AirSim `SimpleFlight`，但不构成 pixel-accurate mask 或 `instance_id=5387` identity proof。
  - `tools/carla_air/audit_actor_bbox_instance_candidates.py` 是只读 actor bbox projection vs raw `instance_synth` 候选审计入口；它用 recorded pose、fixed rig 与 CARLA actor bbox extent 对 PNG candidate id 做几何 sanity ranking，只输出 local/tmp JSON/CSV，不写 masks/tracklets/status。当前 node03 前段与 node04 后段隔离窗口都显示 `44800` 比 `5387` 更几何合理，但仍只是候选，不是 actor-id mapping proof、identity proof 或 pixel-accurate mask。
  - `tools/carla_air/export_actor_pose_projected_bbox_annotations.py` 是 actor pose / bbox 到独立候选 annotation 的诊断入口；默认输出 `masks_actor_bbox`、`tracklets_actor_bbox.json`、`annotation_meta_actor_bbox.json`，source 写为 `carla_actor_pose_projected_bbox`，并拒绝写 `masks_gt`、`masks_synth`、`tracklets.json`、`tracklets_synth.json`。2026-05-29 新增 `--start-index`，窗口基准与 synthetic exporter 一样使用 `trajectory_frame_groups.csv`；node04 后段隔离副本已导出 `masks_actor_bbox_later_window` / `tracklets_actor_bbox_later_window.json` 候选。该输出可用于 node03/node04 bbox-region sanity / debug backprojection，不是 pixel-accurate target mask、identity proof、正式 synthetic target mask 或 final geometry。
  - `tools/carla_air/verify_actor_bbox_candidate_annotations.py` 是 isolated actor-pose projected bbox candidate 的只读 verifier；默认核查 node03 前段 official scene candidate 与 node04 后段 `local/` 隔离副本 candidate 的 `annotation_meta_actor_bbox*`、`annotation_status_actor_bbox*`、`tracklets_actor_bbox*`、mask lineage sha256、object-pose CSV 和 guard flags。它拒绝/标记 `masks_synth`、`tracklets_synth`、`pixel_accurate=true`、`identity_proof=true`、`updates_contract=true` 或 `ready_for_formal_target_masks=true` 等正式化误用；通过只证明 bbox candidate 自洽，不证明 actor-id binding、pixel accuracy、official pipeline input 或 final real geometry。
  - `tools/carla_air/audit_candidate_formalization_readiness.py` 是只读聚合 gate；它汇总官方 POC03 synthetic verifier、scene candidate policy、actor-bbox candidate verifier、semantic-lidar candidate verifier、weak diagnostic ReID smoke verifier、weak writer dry-run、POC03 target-selection readiness、aircraft identity readiness 与 POC03 plan-only readiness，输出 `formalization_ready=false` / `goal_complete=false` 时不修改 scene、contract、mask、tracklet、points、embedding 或 eval。当前输出 `local/carla_air/tmp/candidate_formalization_readiness_after_target_selection_gate_20260529.json` 为 `ok=true`，但 actor-bbox、semantic-lidar、weak writer、trusted target id/tag、multi-identity 与 POC03 execute-to-verify 仍 blocked。
  - `tools/carla_air/audit_actor_id_instance_encoding.py` 是只读 actor id 到 `instance_synth` PNG 编码审计入口；它测试当前 contract `red + 256 * green` 和多种 16/24-bit packed 编码，并可按 actor-bbox ROI 汇总候选值。当前 `local/carla_air/tmp/actor_id_instance_encoding_audit_20260529.json` 结论为 actor id `24` 未出现在测试过的 packed instance-id 编码中；单通道分量值 `24` 只作为 channel diagnostic，不是 actor-id mapping proof。
  - `tools/carla_air/audit_poc03_target_selection_readiness.py` 是只读 POC03 trusted target id/tag gate；它聚合 actor/pose binding、actor-id instance encoding、close actor-relative ROI、mask-quality、CARLA blueprint inventory 与 POC03 plan-only readiness，只输出 local JSON，不选择 target、不执行 conversion、不写 scene/contract/mask/tracklet/points/embedding/eval。2026-05-29 输出 `local/carla_air/tmp/poc03_target_selection_readiness_audit_20260529.json`：`target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`，并分别把 `5387` 与 `44800` 标记为 `trusted_for_formal_conversion=false`、`identity_proof=false`、`pixel_accurate=false`。该 gate 明确阻止把 close-camera ROI、bbox 几何候选或普通 `vehicle.*` calibration 当作 POC03 drone target proof。
  - `tools/carla_air/probe_instance_id_calibration_actor.py` 是 live controlled actor instance-id 校准入口；它临时 spawn 一个受控 CARLA actor 与 instance/semantic cameras，只写 `local/` 诊断 JSON/PNG，不修改 scene outputs、pipeline contract、masks、tracklets、points、embeddings 或 eval。2026-05-29 输出 `local/carla_air/tmp/instance_id_calibration_actor_probe_20260529/report.json`：受控 `vehicle.tesla.model3` actor `id=345` 在 3 帧 ROI 内可由 `green_plus_256_blue` 解码稳定恢复，`roi_actor_id_pixel_count=9532`，但当前 formal conversion contract `contract_red_plus_256_green` 下为 0。随后对 POC03 drone actor `id=24` 复跑 close-view probe 与 fixed-camera actor-id audit，仍未在 tested packed decodes 中发现 `24`，因此该校准只提供 runtime 编码线索，不是 `44800` / `5387` 的 identity proof、pixel-accuracy proof 或正式 synthetic annotation evidence。
  - `tools/carla_air/probe_carla_blueprint_inventory.py` 是只读 CARLA blueprint inventory 诊断入口；它只枚举 runtime blueprint library，不 spawn actor，不写 scene、contract、mask、tracklet、points、embedding 或 eval。2026-05-29 输出 `local/carla_air/tmp/carla_blueprint_inventory_probe_airsim_drone_20260529.json`：CARLA/AirSim 端口在线，`blueprint_count=220`，`exact_blueprint_present["airsim.drone"]=false`，`matched_blueprints_by_filter["airsim.*"]=[]`，`drone` / `uav` / `quad` / `simple` / `flight` / `pawn` token 均无命中；因此当前 runtime 无法通过 CARLA blueprint library spawn 受控 `airsim.drone` actor 做同类 calibration。该结果只记录 blueprint 暴露层 blocker，不证明目标身份、pixel accuracy、formal synthetic annotation 或 final geometry。
  - `tools/carla_air/probe_semantic_lidar_actor_idx.py` 是 live semantic-lidar actor-index 诊断入口；它临时 replay 一行 recorded pose，spawn `sensor.lidar.ray_cast_semantic`，检查 measurement 中的 `object_idx` 是否等于目标 actor id。当前 close-offset probe 可命中 `object_idx=24`，固定三相机位置不命中；该结果是 actor-level 3D source 候选，不是 camera pixel mask、NeoVerse reconstruction 或正式 `points_by_timestamp`。
  - `tools/carla_air/export_semantic_lidar_actor_points.py` 是 live semantic-lidar actor-level points isolated export 入口；它 replay recorded AirSim pose，在 actor-relative offset 位置临时 spawn `sensor.lidar.ray_cast_semantic`，只导出 `object_idx == actor_id` 的点到 caller 指定 `points_by_timestamp` root，source 写为 `carla_semantic_lidar_actor_idx_v1`。默认输出必须在仓库 `local/` 下，非 local 输出需要显式 `--allow-nonlocal-output`；它不写 masks、tracklets、pipeline contract、embeddings 或 eval。该输出只是 diagnostic points candidate，不是 camera pixel mask、fixed-camera geometry、NeoVerse reconstruction 或正式 ReID geometry。
  - `tools/carla_air/verify_semantic_lidar_actor_points_candidate.py` 是 isolated semantic-lidar actor points candidate 的只读 verifier；默认核查 POC03 node03/node04 当前 candidate root 的 `meta.json`、`index.csv`、`points_index.csv`、`.npy` sha256/shape/点数和 diagnostic guard，拒绝 `synthetic_depth_backproject`、`masks_synth`、`tracklets_synth`、camera pixel mask、NeoVerse reconstruction 或 formal scene update 混用。它只证明 candidate root 自洽，不把输出升级为正式 scene input、official verifier 输入或 real geometry。
  - `tools/carla_air/probe_actor_instance_camera_binding.py` 是 live close actor-relative instance/semantic camera 诊断入口；它 replay recorded AirSim pose，在目标 actor 近处临时 spawn `sensor.camera.instance_segmentation` 与 `sensor.camera.semantic_segmentation`，汇总 projected actor bbox ROI 内的 pixel values。默认输出必须在仓库 `local/` 下，非 local 输出需要显式 `--allow-nonlocal-output`；它不写 masks、tracklets、pipeline contract、embeddings 或 eval。该输出只能作为 actor-to-pixel binding 候选证据，不是 fixed-camera capture geometry、pixel-accurate mask、identity proof 或正式 synthetic annotation。
  - `tools/carla_air/probe_actor_gbuffer_binding.py` 是 live CARLA camera GBuffer actor-to-pixel 诊断入口；它 replay 指定 `trajectory_frame_groups.csv` 行，在 fixed camera 或 actor-relative 临时 RGB camera 上尝试通过 `listen_to_gbuffer` 捕获 `SceneStencil`、`CustomDepth`、`CustomStencil` 与可选 `GBufferA`。2026-05-29 新增 `--include-basic-gbuffers`，可额外请求 `SceneColor` / `SceneDepth` 用于区分 GBuffer callback 整体不可用和仅 actor-binding buffer 不可用。默认只写仓库 `local/` 下 report/诊断 artifacts，不写 masks、tracklets、status、pipeline contract、embeddings 或 eval。当前 POC03 node03 row0 actor-relative 与 fixed-cam cam0 basic-buffer probe 均未捕获任何 GBuffer callback，因此该路径暂不能作为 actor-to-pixel evidence。
  - `tools/carla_air/plan_weak_variant_contract_patch.py` 是 weak bbox / semantic-lidar variant 的 plan-only contract patch linter；默认读取 actor-bbox 与 semantic-lidar candidate verifier 输出，生成 proposed weak variant JSON 到 stdout 或仓库 `local/` 下 `--out`，不读取或修改 `pipeline_contract.json`，不写 scene outputs。当前正例 `local/carla_air/tmp/weak_variant_contract_patch_plan_20260529.json` 为 `ok=true`，但 `writes_scene_outputs=false`、`formalization_ready=false`、`goal_complete=false`，仅证明 proposed weak variant 输入自洽。
  - `tools/carla_air/plan_weak_variant_writer_dry_run.py` 是 weak variant writer 前的 dry-run / plan-only 工具；它读取 `weak_variant_contract_patch_plan`，生成未来 additive weak diagnostic variant writer 的待审 JSON，但不读取或修改 `pipeline_contract.json`，不写 masks、tracklets、points、embeddings、eval 或 scene outputs。当前正例 `local/carla_air/tmp/weak_variant_writer_dry_run_plan_20260529.json` 为 `ok=true`、`writes_scene_outputs=false`、`modifies_pipeline_contract=false`、`formalization_ready=false`、`goal_complete=false`，并只给出 official verifier rejection rules 与 downstream opt-in 计划；它仍不是 real writer、official verifier 或 promotion evidence。
  - `tools/carla_air/verify_weak_variant_official_readiness.py` 是 weak variant real writer 前的只读安全门 verifier；它交叉核查 weak writer dry-run、weak diagnostic ReID smoke verifier 与 candidate formalization readiness audit 是否一致保持 non-promotion。当前正例 `local/carla_air/tmp/weak_variant_official_readiness_verification_20260529.json` 为 `ok=true`、`failure_count=0`、`verifier_scope=pre_writer_readiness_only`、`writes_scene_outputs=false`、`modifies_pipeline_contract=false`、`formalization_ready=false`、`goal_complete=false`、`real_writer_implemented=false`、`promotion_evidence_satisfied=false`；它不是 real writer，不修改 contract，不证明 identity / pixel accuracy / final geometry。
  - `tools/carla_air/verify_weak_variant_diag_reid_smoke.py` 是 local-only weak diagnostic ReID smoke 的只读 verifier；它核查 `tracklets_actor_bbox_semantic_lidar_diag.json`、指定 embeddings subdir、semantic-lidar points root、双向 eval 与 `pipeline_contract.json` 防误用，不写 scene outputs。当前基础正例 `local/carla_air/tmp/weak_variant_diag_reid_smoke_verification_20260529.json` 为 `ok=true`、`failure_count=0`、`formalization_ready=false`、`goal_complete=false`；2026-05-29 又新增 `--weak-contract-verification` 检查，可要求 embeddings/eval 的 `weak_variant.contract_verification` 匹配 post-write verifier sha256 与 weak marker 计数，正例为 `local/carla_air/tmp/weak_contract_guard_diag_reid_verification_20260529.json`，旧输出负例为 `local/carla_air/tmp/weak_contract_guard_diag_reid_negative_old_outputs_20260529.json`。
  - `mvp-demo/scripts/carla_air_weak_variant_guard.py` 是 downstream weak diagnostic opt-in 公共 guard；`extract_node_track_embeddings.py` 与 `eval_node_track_retrieval.py` 已接入它。任何路径或 JSON 内容引用 `actor_bbox_semantic_lidar_diag`、`masks_actor_bbox`、`semantic_lidar_actor_points` 或 `carla_semantic_lidar_actor_idx_v1`，都必须显式 `--weak-variant actor_bbox_semantic_lidar_diag`、`--weak-variant-readiness local/carla_air/tmp/weak_variant_official_readiness_verification_20260529.json` 与 `--weak-contract-verification local/carla_air/tmp/weak_variant_contract_update_verification_post_execute_20260529.json`；eval 还要求 weak embeddings 由带 `weak_variant.contract_verification` metadata 的新版 extraction 生成，且 weak eval 输出在 repo `local/` 下。该 guard 只防误用，不是 real writer、promotion evidence 或正式 benchmark。
  - `tools/carla_air/audit_tracklet_input_contract.py` 是下游消费前的只读 tracklet 输入契约审计；它扫描官方 scene 与可选 local tracklet，分类 `proxy_minimal_formal`、`synthetic_depth_backproject_smoke`、`actor_bbox_candidate`、`weak_actor_bbox_semantic_lidar_diagnostic`，并显式保持 `formalization_ready=false` / `goal_complete=false`。该工具不写 scene outputs、不生成 tracklet、不把 proxy / candidate / weak diagnostic tracklet 提升为 final synthetic annotation、identity proof、pixel-accurate mask、final geometry 或 benchmark 输入。
  - `tools/carla_air/check_aircraft_identity_readiness.py` 是 multi-identity benchmark 的只读前置 gate；raw aircraft assets 只进入 inventory，不算 benchmark identity；进入 benchmark roster 前必须有真实 `assets/models/aircraft_normalized/<identity_id>/normalized.fbx`、`preview.png`、`asset_meta.json`，并通过 source/license/axis/unit/bbox/origin metadata 检查；`--min-benchmark-eligible 0` 只允许 inventory pass，不代表 benchmark ready。
  - `tools/carla_air/prioritize_raw_aircraft_normalization.py` 是 raw aircraft normalization 的只读排序工具；默认消费 latest aircraft identity readiness JSON，只输出 `local/` prioritization JSON，不写 normalized assets、scene outputs、contract、mask、tracklet、points、embedding 或 eval；`ok=true` 只表示排序成功，不表示 `benchmark_ready`、`formalization_ready` 或 `goal_complete`。
  - 旧 baseline `20260525_stage3b_cap04_north_bridge` 中 `traj_poc_04_north_bridge` 只能记为整体 gate pass，`node02` 仅 `cam2` 通过；新 live run `20260525_180648_traj_poc_04_north_bridge` 中 `node02` / `node05` 三路 camera 均通过 presence gate，应按 run_id 区分证据。
  - 视频、review overlay、手工 preview 只用于组会与质检，不作为数据集或 ReID 训练输入。

verified_outputs:
  - 2026-05-28 按 runtime 自启动规则启动本地 CARLA-Air 后，CARLA RPC `2000` 与 AirSim RPC `41451` 在线；`run_poc03_synthetic_reid_pipeline.py --execute --stop-after verify --limit 10 --overwrite --run-id 20260528_210808_poc03_synth_depth_reid --source instance --instance-id 5387` 已完成。`instance_id=5387` 只作为 target-candidate gate 输入，不是 identity proof 或 pixel-accuracy proof。
  - POC03 node03/node04 已生成 separate synthetic variant：`tracks/tracklets_synth.json`、`annotations/annotation_meta_synth.json`、`cams/<cam>/masks_synth/*.png`、`mvp-demo/output/carla_air_depth_points/<scene_id>/points_by_timestamp/`、`embeddings_synth_depth_backproject/` 与双向 synthetic eval。`verify_poc03_synthetic_reid_outputs.py` 输出 `local/carla_air/pipeline_runs/20260528_210808_poc03_synth_depth_reid/synthetic_output_verification.json`，`ok=true`、`failure_count=0`。
  - 2026-05-28 live 后 scene audit 输出 `local/carla_air/tmp/scene_pipeline_status_audit_20260528_after_live_synth.json`：官方 scene 共 7 个，5 个仍为 `skeleton_pending_formal_annotations`，2 个 POC03 scene 为 `synthetic_annotation_and_depth_geometry_ready`；`synthetic_annotation_ready_count=2`、`synthetic_depth_points_ready_count=2`、`scene_qc_artifact_count=0`。
  - 2026-05-28 mask-quality diagnostic audit 输出 `local/carla_air/tmp/synthetic_target_mask_quality_audit_20260528.json`：`scene_count=2`、`observation_count=60`、两个 POC03 scene 都触发风险；全局风险包括 `large_mask_area_majority`、`edge_touch_majority`、`near_full_width_bbox_majority`，node03 还触发 `low_proxy_bbox_overlap_majority`。该审计只支持风险诊断，不支持 pixel-accuracy claim。
  - 2026-05-28 QC-only candidate overlay 输出 `local/carla_air/tmp/qc_overlay_node03_instance_5387_with_proxy_ref_20260528.json`、`local/carla_air/tmp/qc_overlay_node03_semantic_14_with_proxy_ref_20260528.json`、`local/carla_air/tmp/qc_overlay_node04_instance_5387_20260528.json` 等：`instance_id=5387` 视觉上覆盖大片天空/背景；小像素 semantic tag 候选更像场景元素或局部小物体，且跨 node 不一致。overlay 仅为 QC，不是正式 pipeline 输入，也不是 identity / pixel-accuracy proof。
  - 2026-05-28 target actor binding live probe 输出 `local/carla_air/tmp/target_actor_binding_probe_replay_seg_20260528_goal_resume_v4.json`：replay POC03 node03 row 0 后，CARLA actor `id=24` / `type_id=airsim.drone` 距 recorded CARLA pose `0.002423m`，AirSim `SimpleFlight` 距 recorded NED `0.002422m`；restore 验证通过，`restore_distance_m=0.001545m`。该证据支持 actor/pose binding，但 AirSim `simSetSegmentationObjectID` 后 `SimpleFlight` 仍查询为 `-1`，不能证明 `instance_id=5387` 或 pixel accuracy。
  - 2026-05-28 actor bbox instance candidate audit 输出 `local/carla_air/tmp/actor_bbox_instance_candidate_audit_20260528_limit10_v2.json` / `.csv`：node03 `44800` 在 30 个 observation 上最贴近 projected actor bbox，`mean_candidate_bbox_iou=0.162635`、`mean_candidate_area_ratio_to_projection=0.162635`、`mean_candidate_center_distance_px=0.459163`；既有 `5387` 为大面积背景型候选，`mean_candidate_bbox_iou=0.000695`、`mean_candidate_area_ratio_to_projection=1525.454545`。node04 同一 limit 下 actor bbox projection 均 outside，不能支持 node04 `5387` target mask。
  - 2026-05-28 actor-pose projected bbox candidate 输出 `local/carla_air/tmp/actor_bbox_annotation_node03_export_20260528.json`：node03 独立导出 `masks_actor_bbox` / `tracks/tracklets_actor_bbox.json` / `annotations/annotation_meta_actor_bbox.json`，`valid_timestamps=10`、三路 camera 各 10 mask、`source=carla_actor_pose_projected_bbox`、`pixel_accurate=false`、`identity_proof=false`、`updates_contract=false`。对应 debug points 在 `local/carla_air/tmp/actor_bbox_depth_points_node03_20260528/points_by_timestamp`，`target_validation_status=not_provided_allow_unvalidated_debug`；node03-only debug embedding shape 为 `(1,161)`。node04 dry-run `local/carla_air/tmp/actor_bbox_annotation_node04_dry_run_20260528.json` 为 `valid_timestamps=0`、`projected_bbox_outside`。官方 verifier `local/carla_air/tmp/poc03_synthetic_verification_after_actor_bbox_candidate_20260528.json` 仍 `ok=true`、`failure_count=0`，说明正式 synthetic 输出未被该候选污染。
  - 2026-05-29 actor-id instance encoding 审计输出 `local/carla_air/tmp/actor_id_instance_encoding_audit_20260529.json` / `.csv`：`status=actor_id_not_observed_in_tested_instance_png_encodings`、`scene_count=2`。node03 读取 30 张 `instance_synth`，有 30 个 projected bbox ROI，6 种 packed instance-id 解码均未出现 actor id `24`；按当前 contract 的 ROI top values 为 `65027`、`59907`、`44800`、`5387`、`7433`。node04 读取 30 张 `instance_synth`，但 `bbox_roi_entries_available=0`，与 projected bbox outside 一致。该审计不修改正式 scene 输出。
  - 2026-05-29 semantic-lidar actor-index 诊断输出 `local/carla_air/tmp/semantic_lidar_actor_idx_probe_node03_row0_range120_20260529.json`：`actor_hit_any=true`、`camera_probe_actor_hit_any=false`、`close_probe_actor_hit_any=true`。固定三相机 long-range probe 均收到 measurement 但 `actor_hit_count=0`；close-above 2m 命中 `object_idx=24` 共 6567 点，close-below 2m 命中 6722 点，`object_tag=42`。该诊断只证明 semantic lidar 可在合适视角返回 actor id 级 3D hits，不构成 camera mask 或正式 ReID geometry。
  - 2026-05-29 semantic-lidar actor points isolated export 输出 `local/carla_air/tmp/semantic_lidar_actor_points_node03_limit10_20260529/points_by_timestamp` 与 `local/carla_air/tmp/semantic_lidar_actor_points_node04_limit10_20260529/points_by_timestamp`：node03 前 10 帧导出 10 个 `.npy`、总点数 54474、单帧 5213-5535 点；node04 前 10 帧导出 10 个 `.npy`、总点数 54094、单帧 5180-5559 点；source 为 `carla_semantic_lidar_actor_idx_v1`，`diagnostic_only=true`、`formal_scene_outputs_modified=false`、`updates_pipeline_contract=false`。官方 verifier `local/carla_air/tmp/poc03_synthetic_verification_after_semantic_lidar_actor_points_pair_20260529.json` 仍 `ok=true`、`failure_count=0`；scene audit `local/carla_air/tmp/scene_pipeline_status_audit_after_semantic_lidar_actor_points_pair_20260529.json` 仍为 7 scenes、2 个 synthetic depth geometry ready、5 个 skeleton pending、QC artifact count 0，说明正式 scene outputs 未被该候选污染。
  - 2026-05-29 semantic-lidar actor points candidate verifier 输出 `local/carla_air/tmp/semantic_lidar_actor_points_candidate_verification_20260529.json`：`ok=true`、`failure_count=0`、`point_root_count=2`。node03 root 为 10 个 timestamp、54474 点，node04 root 为 10 个 timestamp、54094 点；两组均通过 `source=carla_semantic_lidar_actor_idx_v1`、`diagnostic_only=true`、`formal_scene_outputs_modified=false`、`updates_pipeline_contract=false`、`camera_pixel_mask_source=false`、`pixel_accurate_synthetic_annotation=false`、`neoverse_reconstruction=false`、index/points_index/sha256/shape/actor observation 对齐检查。该 verifier 只用于 candidate 防误用，不改变 official scene readiness。
  - 2026-05-29 semantic-lidar actor points verifier 追加 scene lineage guard：默认正例 `local/carla_air/tmp/semantic_lidar_actor_points_candidate_verification_scene_lineage_exit_20260529.json` 为 `ok=true`、`failure_count=0`，并逐 timestamp 反查 scene `trajectory_frame_groups.csv` 与 observation sensor pose；timestamp 篡改负例 `local/carla_air/tmp/semantic_lidar_actor_points_lineage_negative_20260529/report_exit.json` 为 `ok=false`、`failure_count=5` 且默认非零退出。该 guard 不改变其 diagnostic-only / actor-relative / non-NeoVerse 边界。
  - 2026-05-29 close actor-relative instance/semantic camera binding probe 输出 `local/carla_air/tmp/actor_instance_camera_binding_probe_node03_row0_20260529/report.json`、`local/carla_air/tmp/actor_instance_camera_binding_probe_node03_below_limit10_20260529/report.json` 与 `local/carla_air/tmp/actor_instance_camera_binding_probe_node04_below_limit10_20260529/report.json`：node03 row0 上下 probe 为 `observation_count=2` / `capture_ok_count=2`；node03 below limit10 为 `observation_count=10` / `capture_ok_count=10`，ROI `contract_red_plus_256_green` 中 `5387=297499` px、`44800=67892` px、semantic `11=297217` px；node04 below limit10 为 `observation_count=10` / `capture_ok_count=10`，ROI 中 `5387=298083` px、`44800=68077` px、semantic `11=298034` px。该诊断只增强 `44800` 的 close-view 几何候选证据，不证明 actor id `24` 映射到 `44800` 或 `5387`，也不修改正式 scene 输出。
  - 2026-05-29 GBuffer actor-to-pixel 诊断输出 `local/carla_air/tmp/actor_gbuffer_binding_probe_node03_row0_actor_relative_final_20260529/report.json` 与 `local/carla_air/tmp/actor_gbuffer_binding_probe_node03_row0_fixed_cam0_final_20260529/report.json`：CARLA/AirSim 端口均在线，row replay 均成功，actor `24` / `airsim.drone` 与 recorded pose 距离为毫米级；但 actor-relative 与 fixed-cam 均为 `gbuffer_capture_ready=false`、`captured_buffer_count=0`，缺失 `SceneStencil`、`CustomDepth`、`CustomStencil`、`GBufferA`。随后 basic-buffer 复核 `local/carla_air/tmp/actor_gbuffer_binding_probe_node03_row0_actor_relative_basic_20260529/report.json` 与 `local/carla_air/tmp/actor_gbuffer_binding_probe_node03_row0_fixed_cam0_basic_20260529/report.json` 额外请求 `SceneColor` / `SceneDepth`，仍为 `captured_buffer_names=[]`、`basic_gbuffer_capture_ready=false`、`actor_binding_gbuffer_capture_ready=false`。该诊断只记录 blocker，不构成 actor-to-pixel evidence、identity proof、pixel-accurate mask 或正式 synthetic annotation。
  - 2026-05-29 actor-id -> instance-PNG mapping / AirSim segmentation controllability 核查：本地 `PythonAPI/python_api.md` 与 `carla/libcarla.pyi` 未发现将 CARLA actor id 映射或设置为 `sensor.camera.instance_segmentation` PNG encoded value 的 Python API；AirSim `simSetSegmentationObjectID` / `simGetSegmentationObjectID` 仅是 0-255 object/mesh segmentation ID。`local/carla_air/tmp/target_actor_binding_probe_replay_scan_seg_20260529.json` 显示 actor 附近 `SimpleFlight`、`WeatherActor_C_*`、多个 `BP_PIPCamera_C_*`、`ExternalCamera` 的 segmentation id 均为 `-1`；`local/carla_air/tmp/airsim_segmentation_candidate_setget_20260529_clean.json` 对 9 个近邻候选临时 set/get 后 `effective_count=0`。因此当前没有可用的 AirSim object segmentation 控制路径来解释或设置 `44800` / `5387`。
  - 2026-05-29 node04 fixed-camera visibility 复核：前 10 帧本地投影复算显示 actor 在 node04 三路相机前方但在画面左侧之外，camera-forward depth 约 `22.56-27.00m`，projected x 范围约 `cam0=[-781,-645]`、`cam1/cam2=[-762,-630]`；`local/carla_air/tmp/actor_bbox_instance_candidate_audit_20260529_limit120.json` 中 node04 `observation_count=0`、`candidate_count=0`、`skipped_count=287`、`missing_input_count=73`。因此 node04 close-camera `44800` 证据不能直接迁移为 fixed-camera formal mask，当前 raw synthetic window 也不足以支持 node04 bbox/mesh projection formalization。
  - 2026-05-29 node04 later-window isolated fixed-camera synthetic diagnostic：新增 row-window / append-window 后，在普通副本 `local/carla_air/tmp/node04_later_window_scene_fullcopy_20260529/` 上补跑 `start_index=93`、`limit=20`，覆盖 `84921711-86821711`；export meta `sensor_export_id=20260529_node04_later_window_isolated_export`、`rows_requested=20`、`rows_written=180`、`failures=[]`。候选检查 `local/carla_air/tmp/node04_later_window_scene_fullcopy_20260529_candidates.json` 为 `sensor_images_read=120`、`missing_inputs_count=0`；bbox ROI 审计 `local/carla_air/tmp/node04_later_window_actor_bbox_instance_candidate_audit_20260529.json` 为 `observation_count=52`、`candidate_count=8`，top candidate `44800`：`observation_count=46`、`timestamp_count=18`、`cam_count=3`、`mean_candidate_bbox_iou=0.412969`、`mean_candidate_area_ratio_to_projection=0.412969`、`mean_candidate_center_distance_px=3.074031`、`projection_match_score=0.36775`。该诊断只证明 node04 后段 fixed-camera 窗口存在更强 `44800` 几何候选，不证明 actor id `24` 映射到 `44800`，不写正式 masks/tracklets/points/eval。官方 verifier `local/carla_air/tmp/poc03_synthetic_verification_after_isolated_node04_later_export_20260529.json` 仍 `ok=true`、`failure_count=0`；scene audit `local/carla_air/tmp/scene_pipeline_status_audit_after_isolated_node04_later_export_20260529.json` 仍为 7 scenes、2 个 synthetic ready、5 个 skeleton pending、`scene_qc_artifact_count=0`，说明正式 scene 未被隔离诊断污染。
  - 2026-05-29 node04 later-window actor-bbox candidate export：`export_actor_pose_projected_bbox_annotations.py --start-index 93 --limit 20` 在隔离副本 `local/carla_air/tmp/node04_later_window_scene_fullcopy_20260529/` 写出 `cams/<cam>/masks_actor_bbox_later_window/*.png`、`tracks/tracklets_actor_bbox_later_window.json` 与 `annotations/annotation_meta_actor_bbox_later_window.json`；`local/carla_air/tmp/node04_later_window_actor_bbox_annotation_export_20260529.json` 为 `valid_timestamps=15`、45 张 mask、`source=carla_actor_pose_projected_bbox`、`pixel_accurate=false`、`identity_proof=false`、`updates_contract=false`、`writes_formal_target_masks=false`。该结果只是 isolated bbox rectangle candidate，不是正式 `masks_synth`、identity proof、pixel-accurate annotation 或 real geometry。官方 verifier `local/carla_air/tmp/poc03_synthetic_verification_after_node04_later_bbox_candidate_20260529.json` 仍 `ok=true`、`failure_count=0`；scene audit `local/carla_air/tmp/scene_pipeline_status_audit_after_node04_later_bbox_candidate_20260529.json` 仍为 7 scenes、2 个 synthetic ready、5 个 skeleton pending、`scene_qc_artifact_count=0`。
  - 2026-05-29 candidate artifact guard 审计输出 `local/carla_air/tmp/scene_pipeline_status_audit_with_candidate_guard_20260529.json`：官方 scene 状态仍为 7 scenes、2 个 synthetic ready、5 个 skeleton pending、`scene_qc_artifact_count=0`；新增 `candidate_artifact_policy.formal_inputs_consumed=false`，并检测到 1 个 official scene 有 actor-bbox candidate/debug artifacts、共 35 个候选产物。该统计只用于可见性，不改变正式 scene readiness。
  - 2026-05-29 继续执行状态报告已按 fresh audits 更新为 `research/reports/carla_air_goal_continue_status_2026_05_29_zh.md`：`local/carla_air/tmp/poc03_target_selection_readiness_goal_continue_20260529.json` 仍 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`；`local/carla_air/tmp/goal_execution_matrix_goal_continue_20260529.json` 与 read-only rerun 仍显示 formal conversion、formal NeoVerse、multi-identity roster 三条路线均不可执行；`local/carla_air/tmp/poc03_synthetic_verification_goal_continue_latest_20260529.json` 仍 `ok=true`、`failure_count=0`，但只证明 POC03 separate synthetic depth-backprojection smoke 自洽；`local/carla_air/tmp/neoverse_geometry_readiness_goal_continue_20260529.json` 仍 `formal_neoverse_ready=false`、`formal_neoverse_output_count=0`；`local/carla_air/tmp/aircraft_identity_readiness_goal_continue_20260529.json` 仍 `benchmark_ready=false`、`normalized_inventory.benchmark_eligible_count=0`。本次刷新不改变结论：proxy、bbox candidate、semantic-lidar candidate、GBuffer / raycast diagnostics 与 depth-backprojection baseline 均不能升级为 identity proof、pixel-accurate mask、official pipeline input、NeoVerse reconstruction 或 final real geometry。
  - 2026-05-29 actor-bbox candidate annotation verifier 输出 `local/carla_air/tmp/actor_bbox_candidate_annotation_verification_20260529.json`：`ok=true`、`failure_count=0`、`candidate_count=2`。node03 前段 candidate 为 10 个 timestamp / 30 张 mask；node04 后段 isolated candidate 为 15 个 timestamp / 45 张 mask；两组均为 `source=carla_actor_pose_projected_bbox`、`pixel_accurate=false`、`identity_proof=false`、`updates_contract=false`、`writes_formal_target_masks=false`。负例 `local/carla_air/tmp/actor_bbox_candidate_annotation_verification_negative_formal_tracklets_20260529.json` 故意使用 `tracks/tracklets_synth.json` 并按预期 `ok=false`。该 verifier 只防误用，不改变 official scene readiness。
  - 2026-05-29 POC03 target-selection readiness gate 输出 `research/reports/carla_air_poc03_target_selection_readiness_gate_2026_05_29_zh.md` 与 `local/carla_air/tmp/poc03_target_selection_readiness_audit_20260529.json`：新增 `tools/carla_air/audit_poc03_target_selection_readiness.py` 聚合 actor binding、actor-id audit、close ROI、mask-quality、blueprint inventory 与 plan-only readiness；正例 `ok=true` 但 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`，`5387` 与 `44800` 均为 `identity_proof=false`、`pixel_accurate=false`。该 gate 不选择 target、不执行 conversion、不写正式输出，只把“当前无可信 target id/tag”变成可复跑 blocker。
  - 2026-05-29 candidate formalization readiness 聚合审计输出 `local/carla_air/tmp/candidate_formalization_readiness_after_target_selection_gate_20260529.json`：`ok=true`、`formalization_ready=false`、`goal_complete=false`。它确认官方 POC03 synthetic smoke 与 scene candidate policy 仍自洽，并消费 weak writer dry-run 与 target-selection readiness；weak writer dry-run 为 plan-only blocker，target-selection gate 明确缺 trusted target id/tag。actor-bbox candidate 缺 actor-to-pixel / pixel-accuracy evidence，semantic-lidar actor points 仍是 actor-relative diagnostic geometry，multi-identity benchmark `benchmark_eligible_count=0`，POC03 execute-to-verify 仍 blocked；因此候选不能升级为正式 synthetic annotation、final real geometry 或 benchmark 完成证据。
  - 2026-05-29 weak variant contract plan 输出 `research/reports/carla_air_weak_variant_contract_plan_2026_05_29_zh.md`：bbox / semantic-lidar 可以设计独立 weak variant contract，但当前仍为 plan-only，`formalization_ready=false`、`goal_complete=false`。禁止复用 `masks_synth`、`tracklets_synth`、`annotation_meta_synth`、`synthetic_target_masks` 或 `synthetic_depth_backproject`；下一步只允许先做 plan-only / dry-run contract linter，在 `local/` 生成 proposed patch JSON，由主 agent review 后再决定是否进入 writer 阶段。
  - 2026-05-29 weak variant contract patch linter 输出 `local/carla_air/tmp/weak_variant_contract_patch_plan_20260529.json`：`ok=true`、`writes_scene_outputs=false`、`formalization_ready=false`、`goal_complete=false`。proposed bbox weak variant 汇总 2 个 candidate、25 个 timestamp、75 张 mask；semantic-lidar weak variant 汇总 2 个 root、20 个 timestamp、108568 点。负例确认缺输入时 `ok=false` 并非零退出，非 `local/` 输出默认被拒绝。该 linter 不是 writer，不改变 official scene readiness。
  - 2026-05-29 weak variant writer dry-run 输出 `research/reports/carla_air_weak_variant_writer_dry_run_2026_05_29_zh.md` 与 `local/carla_air/tmp/weak_variant_writer_dry_run_plan_20260529.json`：`ok=true`、`writes_scene_outputs=false`、`modifies_pipeline_contract=false`、`formalization_ready=false`、`goal_complete=false`。dry-run 仅为 node03/node04 生成 additive weak diagnostic variant 的待审计划、official verifier 拒绝规则和 downstream opt-in 参数；负例确认非 `local/` 输出默认拒绝，输入 plan 若把 `formalization_ready=true` 则失败。该工具不是 real writer，不修改 official scene contract，不把 bbox / semantic-lidar candidate 升级为 formal synthetic annotation、identity proof、pixel-accurate mask、real/final geometry、NeoVerse reconstruction 或 benchmark evidence。
  - 2026-05-29 weak variant official-readiness verifier 输出 `research/reports/carla_air_weak_variant_official_readiness_verifier_2026_05_29_zh.md` 与 `local/carla_air/tmp/weak_variant_official_readiness_verification_20260529.json`：`ok=true`、`failure_count=0`、`verifier_scope=pre_writer_readiness_only`、`writes_scene_outputs=false`、`modifies_pipeline_contract=false`、`formalization_ready=false`、`goal_complete=false`、`real_writer_implemented=false`、`promotion_evidence_satisfied=false`。它只证明 weak writer dry-run、weak diagnostic ReID smoke verifier 与 readiness audit 三者的 non-promotion guard 自洽；负例确认非 `local/` 输出、dry-run 篡改为修改 contract、readiness 缺失 weak writer summary 都会失败。该 verifier 不关闭 actor-to-pixel、pixel-accuracy、final geometry 或 multi-identity blocker。
  - 2026-05-29 local-only weak diagnostic ReID smoke 输出 `research/reports/carla_air_weak_variant_diag_reid_smoke_2026_05_29_zh.md`：在 `local/` 隔离副本中用 actor-bbox candidate masks + `carla_semantic_lidar_actor_idx_v1` actor-level points 构造 `tracklets_actor_bbox_semantic_lidar_diag.json`，node03 为 10 个 timestamp / 30 张 bbox mask / 10 个 fused points 文件，node04 later-window 为 15 个 timestamp / 45 张 bbox mask / 15 个 fused points 文件；`embeddings_actor_bbox_semantic_lidar_diag` 双侧 shape 均为 `[1,161]`，双向 eval `local/carla_air/tmp/weak_variant_diag_eval_node03_to_node04_20260529.json` 与 `...node04_to_node03_20260529.json` 均为 `mAP=1.0`、`recall_at_1/5/10=1.0`、`num_queries=1`、`num_gallery=1`。新增 verifier `local/carla_air/tmp/weak_variant_diag_reid_smoke_verification_20260529.json` 为 `ok=true`、`failure_count=0`，并确认 `diagnostic_only=true`、`formal_scene_outputs_modified=false`、`updates_pipeline_contract=false`、`formalization_ready=false`、`goal_complete=false`。该结果只证明 isolated weak-route operability，不更新 official `pipeline_contract.json`，不替代 `masks_synth` / `tracklets_synth`，不是 identity proof、pixel-accurate mask、final real geometry、NeoVerse reconstruction、正式 benchmark 或 goal completion 证据。
  - 2026-05-29 weak variant downstream guard 输出 `research/reports/carla_air_weak_variant_downstream_guard_2026_05_29_zh.md`：`mvp-demo/scripts/carla_air_weak_variant_guard.py` 新增公共 guard，embedding/eval 下游脚本默认拒绝 weak diagnostic tracklets / embeddings；正例在 `local/` 生成 `embeddings_actor_bbox_semantic_lidar_diag_guarded_20260529`，双向 guarded eval `local/carla_air/tmp/weak_variant_diag_eval_node03_to_node04_guarded_20260529.json` 与 `...node04_to_node03_guarded_20260529.json` 均为 `mAP=1.0`、`recall_at_1=1.0`；负例确认无 `--weak-variant`、无 readiness、坏 readiness、旧 weak embeddings metadata、weak eval 输出到 `/tmp` 均被拒绝。该结果只补 downstream opt-in guard，不写 official contract，不实现 protected real writer，不解除 `formalization_ready=false` / `goal_complete=false`。
  - 2026-05-29 weak variant downstream contract guard 输出 `research/reports/carla_air_weak_variant_downstream_contract_guard_2026_05_29_zh.md`：weak diagnostic embedding/eval 现在还必须提供 post-write contract verifier JSON，并把 `weak_variant.contract_verification` evidence 写入/核验。正例 embeddings subdir 为 `embeddings_actor_bbox_semantic_lidar_diag_contract_guarded_20260529`；双向 eval `local/carla_air/tmp/weak_contract_guard_eval_node03_to_node04_20260529.json` 与 `...node04_to_node03_20260529.json` 均为 `num_queries=1`、`num_gallery=1`、`mAP=1.0`、`recall_at_1=1.0`，且 metadata 匹配 `weak_variant_contract_update_verification_post_execute_20260529.json` 的 sha256。verifier 正例 `local/carla_air/tmp/weak_contract_guard_diag_reid_verification_20260529.json` 为 `ok=true`；旧 weak outputs 负例 `local/carla_air/tmp/weak_contract_guard_diag_reid_negative_old_outputs_20260529.json` 为 `ok=false`、`failure_count=8`。该结果只是 provenance / non-promotion guard，不是 formal benchmark 或 promotion evidence。
  - 2026-05-29 weak variant contract-update verifier 输出 `research/reports/carla_air_weak_variant_contract_update_verifier_2026_05_29_zh.md` 与 `local/carla_air/tmp/weak_variant_contract_update_verification_20260529.json`：新增 `tools/carla_air/verify_weak_variant_contract_update.py` 只读扫描官方 7 个 `pipeline_contract.json`、weak writer dry-run 与 official readiness；正例 `ok=true`、`failure_count=0`、`registered_weak_variant_count=0`、`weak_marker_scene_count=0`，确认 official contract 当前未登记或污染 weak diagnostic variant。负例确认 dry-run 篡改为 `formalization_ready=true` 会失败，weak marker 写入 `synthetic_target_masks` / `synthetic_annotations.target_masks` 会失败。该 verifier 补齐 contract/update 污染扫描安全门，但不写 scene outputs、不修改 `pipeline_contract.json`、不实现 protected real writer、不提供 promotion evidence，也不解除 actor-to-pixel、pixel accuracy、final geometry 或 multi-identity blocker。
  - 2026-05-29 protected weak contract writer dry-run 输出 `research/reports/carla_air_weak_variant_contract_update_writer_dry_run_2026_05_29_zh.md` 与 `local/carla_air/tmp/weak_variant_contract_update_writer_dry_run_20260529.json`：新增 `tools/carla_air/apply_weak_variant_contract_update.py`，默认只生成待审 `weak_diagnostic_variants` contract diff，不修改 official contracts；正例 `ok=true`、`dry_run=true`、`planned_update_count=2`、`written_update_count=0`，计划仅覆盖 POC03 node03/node04 的 `actor_pose_projected_bbox_weak` 与 `semantic_lidar_actor_idx_weak`。负例 `--execute` 缺少 `--enable-official-contract-update` 与 verifier sha256 时 `ok=false`、`written_update_count=0`；随后 contract-update verifier 仍 `registered_weak_variant_count=0`。该 writer scaffold 只提供受保护显式 enable 路径，不表示 real contract integration 已执行，不提供 promotion evidence，不解除 `formalization_ready=false` / `goal_complete=false`。
  - 2026-05-29 tracklet input contract 审计输出 `research/reports/carla_air_tracklet_input_contract_audit_2026_05_29_zh.md`、`local/carla_air/tmp/tracklet_input_contract_audit_20260529.json` 与 `local/carla_air/tmp/tracklet_input_contract_audit_with_weak_diag_20260529.json`：默认扫描 `ok=true`、`formalization_ready=false`、`goal_complete=false`、`tracklet_count=5`；加入本地 weak diagnostic tracklet 后 `tracklet_count=6`，新增 `weak_actor_bbox_semantic_lidar_diagnostic=1`。该审计不生成或修改 tracklet，不进行 formal promotion；proxy / synthetic smoke / actor-bbox / weak diagnostic 全部保持非 final annotation、非 final geometry、非 benchmark 输入。
  - 2026-05-29 instance PNG mapping 本地 API 审计输出 `research/reports/carla_air_instance_png_mapping_local_api_audit_2026_05_29_zh.md`：`sensor_gallery.py` 明确支持 `object_id = R + 256*G (BGRA)` / `B = semantic tag` 的 decode 来源，但本地 `python_api.md`、`libcarla.pyi` 与 AirSim segmentation set/get 证据仍未提供 actor id -> instance PNG value 的 setter 或 lookup。该审计不运行 runtime、不写 scene outputs；它只巩固当前 decode contract，不证明 actor `24` 映射到 `44800` / `5387`。
  - 2026-05-29 instance decode inspect 诊断增强输出 `research/reports/carla_air_instance_decode_diagnostic_inspect_enhancement_2026_05_29_zh.md`：`inspect_synthetic_sensor_ids.py` 新增默认关闭的 `--include-instance-decode-diagnostics`，用于在 `instance_decode_diagnostics` 下报告 alternate packed decode / single-channel component summaries；默认 inspect 输出不含该字段，开启后 node03 2 帧读取得到 10 个 decode summaries，且默认/开启诊断两份 JSON 的 `candidates.instance` 完全一致。负例 `local/carla_air/tmp/validate_decode_diagnostic_not_formal_candidate_negative_20260529.json` 为 `ok=false`、`failure_count=1`、失败项 `target_candidate_present`，确认 alternate decode value 不会被 formal validation 当作正式候选；正例 `local/carla_air/tmp/validate_decode_diagnostic_formal_candidate_positive_20260529.json` 为 `ok=true`，确认既有正式 candidate validation 兼容该新增诊断字段。`run_poc03_synthetic_reid_pipeline.py` 同步新增 plan/inspect passthrough `--inspect-instance-decode-diagnostics`，但不改变 convert/points/eval/verify 语义。
  - 2026-05-29 annotation API surface live probe 输出 `research/reports/carla_air_annotation_api_surface_probe_2026_05_29_zh.md` 与 `local/carla_air/tmp/carla_annotation_api_surface_probe_20260529.json`：actor `24` / `airsim.drone` 可见，但 actor 侧只暴露 `semantic_tags` 且无 setter；world 侧只暴露 `set_annotations_traverse_translucency`；instance / semantic / depth camera 与 semantic lidar blueprint 均无 instance、segmentation、custom 或 stencil 相关可设 attribute。该证据已接入 `local/carla_air/tmp/poc03_target_selection_readiness_after_api_surface_20260529.json`，gate 仍 `target_selection_ready=false`、`trusted_target_id_or_tag_available=false`、`ready_for_formal_conversion=false`，并把无公开 annotation setter 作为新增 blocker；`local/carla_air/tmp/goal_execution_matrix_after_api_surface_20260529.json` 仍显示三条正式路线不可执行。

blockers:
  - 本机缺少 Blender / UE / FBX 工具链，当前无法完成 normalized asset 规范化与自定义 aircraft 替换实测。
  - UE/CARLA import/readback 已外部化：当前 blocker 不是继续在本机构建 UE4Editor，而是等待外部机器提供真实 UE import smoke、CARLA spawn/readback、per-identity asset/blueprint path 与可审计 evidence patch；在证据返回前，full 6-identity live dataset 不能 promotion。
  - Multi-identity benchmark 当前没有任何 eligible normalized identity：`local/carla_air/tmp/aircraft_identity_readiness_after_target_gate_20260529.json` 中 `ok=false`、`benchmark_ready=false`、`normalized_inventory.identity_count=0`、`normalized_inventory.benchmark_eligible_count=0`；raw asset inventory 不能替代 normalized benchmark roster。`local/carla_air/tmp/raw_aircraft_normalization_prioritization_20260529.json` 只给出 normalization 候选排序，top candidates 为 `dji_drone_fbx_abc`、`dji_inspire2_fbx`、`dji_tech_drone_gltf`，仍不生成或证明 normalized identity。
  - 真正用于最终数据集的 pixel-accurate mask/depth/object pose 与 NeoVerse 真 4D geometry 仍未接入；当前 live smoke 是 `carla_instance_segmentation_camera_target_instance` target-mask + `carla_depth_synth_mask_backprojection_v1` 非 proxy baseline，不是 pixel-accurate identity proof，也不是 NeoVerse reconstruction。
  - 2026-05-29 NeoVerse geometry readiness audit 明确 `formal_neoverse_ready=false`：当前 `mvp-demo/output/neoverse_fused` 两个 POC03 root 仍是 `carla_air_minimal_formal_proxy_box_surface_v1` proxy points，`mvp-demo/output/carla_air_depth_points` 两个 root只是 `carla_depth_synth_mask_backprojection_v1` baseline。2026-05-30 已确认 `third_party/NeoVerse/models/NeoVerse/reconstructor.ckpt` 通过软链接接入默认路径并解析为 `6039277930` bytes 目标文件；但没有任何当前 `points_by_timestamp` root 可被记录为 formal NeoVerse / final real geometry。
  - POC03 mask-quality audit 对当前 `instance_id=5387` 输出新增强 blocker：两个 scene 均多数 mask 面积偏大、贴边、bbox 近全宽；node03 还与 proxy bbox 多数低重叠。当前 verifier 仍可支持 validated synthetic annotation / non-proxy depth-backprojection smoke，但不能消除 target identity / pixel-accuracy 风险。
  - POC03 candidate overlay 进一步支持该 blocker：`instance_id=5387` 更像 sky/background segmentation candidate；当前小像素 semantic tag 线索不足以直接替换为 drone target。必须补 simulator-side actor/segmentation id 绑定，或让目标 actor 拥有可追踪、可解释的 segmentation/instance id 后再重跑 conversion。
  - Target actor / pose binding 已有强证据，但 pixel-level annotation source 仍未解决：当前 AirSim segmentation id mutation 对 `SimpleFlight` 及 9 个近邻候选 object 都不产生可查询 id，且没有证据把 CARLA actor `24` / AirSim `SimpleFlight` 映射到既有 `instance_id=5387` 或 `44800` PNG mask。
  - 2026-05-29 actor-id instance encoding 审计没有在当前 `instance_synth` PNG 的测试 packed 编码中发现 actor id `24`；这进一步说明当前 raw PNG 不能直接提供 actor id -> target mask 的闭环。单通道分量值 `24` 出现不等于 actor-id binding proof。
  - 2026-05-29 controlled actor instance-id calibration 显示普通 CARLA actor `id=345` 可由 `green + 256 * blue` 从 instance camera ROI 中恢复，但当前正式 conversion contract `red + 256 * green` 不能恢复该 id；同一替代解码复核 POC03 close-view drone ROI 得到 `61871` / `35093` 等候选而非 `24`，fixed-camera audit 仍为 `actor_id_not_observed_in_tested_instance_png_encodings`。因此该结果只能指导后续 decode/UE 映射排障，不能升级 `44800` / `5387` 或关闭 actor-to-pixel blocker。
  - 本地 `sensor_gallery.py` 支持 `red + 256 * green` 作为 CARLA instance PNG decode contract，但这只是 PNG value 解码规则，不是 actor id 映射表；仍没有 API 或本地表能把 actor `24` 设置/查询为 `44800`、`5387` 或其他 fixed-camera candidate value。
  - 2026-05-29 受控 `airsim.drone` blueprint spawn 路线被 runtime blueprint inventory 阻断：`probe_instance_id_calibration_actor.py --actor-blueprint-filter 'airsim.*' --preferred-actor-blueprint airsim.drone` 因 `No CARLA actor blueprints matched: airsim.*` 失败；`probe_carla_blueprint_inventory.py` 复核 `airsim.drone` 不存在、`airsim.*` match 为 0。该结果说明 AirSim drone live actor 虽可在世界 actor 列表中绑定，但不作为可 spawn CARLA blueprint 暴露，不能用普通 controlled actor calibration 直接覆盖 POC03 drone `actor_id=24`。
  - 2026-05-29 target-selection readiness gate 明确当前没有可信 target id/tag：`5387` 因 actor id 未映射、mask-quality 风险、protected plan 未选择 target 而不可正式 conversion；`44800` 虽在 close ROI 与 bbox sanity 中更像几何候选，但仍缺 actor-id binding、protected plan target 选择与 pixel-accuracy evidence。该 gate 不关闭 blocker，只防止误把 candidate value 当 proof。
  - 2026-05-29 instance decode diagnostics 已接入 inspect / POC03 plan-only 入口，但它只让 alternate decode 证据可见，不改变正式 `red + 256 * green` candidate contract；验证确认 alternate decode value 不会被 target validation 消费。因此该增强不关闭 actor-to-pixel blocker，也不能把 `44800`、`5387`、`61871`、`35093` 或任何单通道 component 值升级为 identity proof、pixel-accurate mask 或 real geometry。
  - 2026-05-29 CARLA camera GBuffer 支线已实测但暂不可用：虽然 Python API 暴露 `GBufferTextureID` / `listen_to_gbuffer`，但 POC03 node03 row0 的 actor-relative 与 fixed-cam probe 均未收到 `SceneStencil`、`CustomDepth`、`CustomStencil` 或 `GBufferA` callback；追加 `SceneColor` / `SceneDepth` basic-buffer probe 后仍没有任何 GBuffer callback。当前 CARLA-Air runtime/Python GBuffer 路径不能作为 actor-to-pixel evidence。
  - 2026-05-29 CARLA raycast / project_point 支线已实测为 geometry/occlusion sanity：`World.cast_ray` 与 `World.project_point` 可调用，但本地 API 只返回 `LabelledPoint` 的 semantic label / location，不返回 actor id；POC03 node03 row0 fixed-camera sample 返回 `Buildings` / `None`。因此该支线不能关闭 actor-to-pixel blocker，也不能把 `44800`、`5387` 或 projected bbox 升级为 identity proof / pixel-accurate mask / formal annotation。
  - 2026-05-29 annotation API surface live probe 进一步确认公开 Python API 缺口：actor `24` 只暴露 `semantic_tags`，没有 actor-level annotation setter；world 只暴露 `set_annotations_traverse_translucency`；相关 sensor blueprint 没有 instance / segmentation / custom / stencil 可设 attribute。因此当前不能通过公开 CARLA Python API 直接给 POC03 drone 设置或查询 instance PNG value / segmentation id / custom stencil，不能关闭 actor-to-pixel blocker。
  - 2026-05-29 annotation source bridge live probe 已新增 bridge summary 与 live instance camera smoke：offline bridge check 仍保持 `raw_carla_depth_semantic_instance_camera_available="unknown"`，live probe 在 `Town10HD --opengl --quality Low --fg` 下确认 CARLA depth / semantic / instance 三个 sensor 均可 smoke，AirSim `Segmentation` / `DepthPlanar` 亦可返回；但该结果仍只是 source availability / bridge evidence，不是 target identity proof、pixel accuracy proof 或 formal synthetic annotation，`formal_conversion_still_requires_target_selection=true`、`ready_for_formal_conversion=false`、`formal_neoverse_ready=false`。
  - node03 `44800` 是当前几何 sanity 最强的 `instance_synth` 候选；close actor-relative camera probe 又显示 node03/node04 的 projected actor ROI 内都稳定出现 `44800` 小区域；node04 后段隔离 fixed-camera window 也显示 `44800` 是 bbox ROI 几何最强候选。但这些证据仍缺 actor-id binding / pixel-accuracy proof，不能直接用 `44800` 替换正式输出；必须先补 target validation 与更强 actor-to-pixel 证据，或把 bbox/mesh/semantic-lidar 路线设计成明确 source 的独立正式化方案。
  - actor-pose projected bbox candidate 当前在 node03 前段窗口和 node04 后段隔离窗口均已形成可消费候选；node04 后段候选为 isolated `masks_actor_bbox_later_window` / `tracklets_actor_bbox_later_window.json`，未更新正式 contract，也不得替换正式 `masks_synth` 或 `tracklets_synth`。该候选只能作为 bbox-region diagnostic，不是 pixel-accurate annotation、identity proof 或 final 4D geometry。

latest_mask_gt_decoupling_strategy: 2026-05-30 本轮把主线与 `mask_gt` 进一步拆开：`Dataset Generation Pipeline V1` 继续作为主线推进，但 `mask_gt` 只保留为独立、有限范围的审计 / 可插拔 label source，不纳入默认 promotion。节点相机 probe 已确认 `node03` / `node04` 的 `cam0` / `cam1` / `cam2` 可稳定拿到 CARLA `RGB`、`semantic`、`instance`、`depth` source；但 tested packed instance decode 仍未命中 `actor_id=24`，因此当前还不能把节点相机输出直接升级成正式 `mask_gt`。AirSim onboard camera 不作为数据集 mask 来源，proxy / candidate / pseudo 继续不得写成 `mask_gt`。本轮实现后，existing-scene index 可在 `mask_gt.availability=unavailable` 下生成 360 条 node04 样本；现有 `masks_gt` 目录被视为 legacy/proxy/bootstrap 候选，只有未来显式 `annotations/*mask_gt*_evidence.json` 同时声明 `is_mask_gt=true`、`formal_mask_gt_ready=true`、`pixel_accurate=true` 时才允许升级。
  - Semantic-lidar actor-index 已证明 close-offset / actor-relative actor-level 3D hits 可行，并已能为 POC03 node03/node04 各导出 isolated `carla_semantic_lidar_actor_idx_v1` points candidate；但固定三相机位置当前没有 `object_idx=24` 命中，actor-relative placement 不等于 fixed-camera capture geometry，且该输出没有接入 `tracklets_synth` / official verifier。因此它尚不能替换正式 `points_by_timestamp`，需要解决 sensor placement / aggregation / actor-local geometry capture 的正式化设计后才能作为 real geometry 支线推进。
  - 2026-05-29 已冻结候选正式化边界：bbox rectangle 与 semantic-lidar actor points 只能通过独立 source/status/variant key 接入，必须保留 `pixel_accurate=false` 与 `identity_proof=false`，除非另有可审计 actor-to-pixel / pixel-accuracy 证据；不得覆盖 `synthetic_target_masks`、`synthetic_depth_backproject`、`masks_synth` 或 `tracklets_synth`。
  - 2026-05-29 聚合 readiness gate 已明确 `formalization_ready=false`、`goal_complete=false`；在 actor-bbox / semantic-lidar / identity readiness / POC03 target selection blocker 未解除前，不启动 candidate formal promotion、NeoVerse final geometry 宣称或 multi-identity benchmark。
  - weak variant contract plan 已明确 candidate 可设计但不得 promotion；任何 bbox / semantic-lidar writer 必须先经过独立 dry-run linter、独立 namespace、独立 verifier 与 downstream opt-in，不得默认进入 official synthetic target mask 或 depth-backproject route。
  - weak variant contract patch linter 只生成 `local/` proposed patch JSON；即使 `ok=true`，仍保留 actor-to-pixel evidence missing、semantic-lidar actor-relative placement not formal、promotion evidence missing 三个 blocker。
  - weak variant official-readiness verifier 已补齐 real writer 前的只读安全门，但只验证 dry-run / diagnostic / readiness 的 non-promotion guard 自洽；即使 `ok=true`，仍保留 promotion evidence missing、actor-to-pixel evidence missing、pixel accuracy missing、final geometry missing 与 multi-identity missing blocker，也不解除 `formalization_ready=false` / `goal_complete=false`。
  - weak variant downstream guard 已阻止下游默认消费 weak diagnostic 输出；后续 guarded weak embedding/eval 还必须携带 post-write contract verification evidence。但它只是消费侧防误用；即使 guarded weak eval / verifier `ok`，也仍是 1 query vs 1 gallery 的 local diagnostic，不是 formal benchmark 或 promotion evidence。
  - weak variant contract-update verifier 已补齐 official contract / future update 的只读污染扫描安全门；即使 `ok=true`，也只证明 weak marker 位于允许 namespace、readiness 仍 non-promotion，不是 promotion evidence 或 formal benchmark。
  - protected weak contract writer 已在 post-write verifier 保护下执行并登记 weak diagnostic variants；但该登记只位于 `weak_diagnostic_variants` namespace，不是 `tracklets_synth` / `masks_synth` / formal scene input、promotion evidence 或 benchmark evidence。
  - weak diagnostic ReID smoke 虽已跑通双节点检索闭环，但样本量只有 1 query vs 1 gallery，且同时依赖 bbox rectangle candidate 与 actor-relative semantic-lidar candidate；它不能解除 actor-to-pixel evidence missing、pixel-accuracy missing、fixed-camera/final geometry missing、promotion evidence missing 或 multi-identity missing blocker。
  - tracklet input contract audit 已能在下游消费前区分 proxy / synthetic smoke / actor-bbox candidate / weak diagnostic tracklet，但它只是防误用 preflight；即使 `ok=true`，也不表示 formalization ready、goal complete、pixel-accurate annotation、final geometry、NeoVerse reconstruction 或 multi-identity benchmark ready。
  - POC03 node03/node04 的 primary proxy outputs 仍保留为 `tracks/tracklets.json` 与 `mvp-demo/output/neoverse_fused/...`；正式叙述必须引用 separate synthetic variant，不能把 proxy annotation / proxy points 记作 synthetic annotation / real geometry。
  - 2026-05-29 goal execution matrix 将正式三条路线固定为不可执行：target-to-pixel 缺 trusted id/tag，NeoVerse 缺 formal root/checkpoint，multi-identity 缺 benchmark-eligible normalized identity；weak diagnostic 只能显式 opt-in 诊断，不能 promotion。
  - 2026-05-29 UE/CARLA import readiness guard 进一步明确 multi-identity benchmark blocker：3 个 local POC 虽然 Assimp import smoke 通过，但 `ue_import_tested=false`、`ue_carla_import_verified=false`；当前只找到 packaged `CarlaUE4-Linux-Shipping` 与 `CarlaUE4.uproject`，没有 `UE4Editor` / `UnrealEditor` import toolchain，因此不能把 local POC roster 升级为 formal benchmark roster。
  - 2026-05-29 formal target-selection guard 已把 `target_candidate_gate_passed_not_identity_proof` 与 formal output 写入解耦：candidate validation 通过不再足以写 `masks_synth` / `tracklets_synth` 或 formal depth-backprojection points；必须另有 target-selection readiness report 证明 actor-to-pixel identity 与 pixel accuracy。当前 readiness 仍 false，因此正式 conversion / points / NeoVerse / benchmark 仍不可执行。
  - 2026-05-29 synthetic verifier 也已同步收紧：旧 POC03 `tracklets_synth` / `annotation_meta_synth` / depth-backprojection points 因缺 `target_selection_readiness` 会被 verifier 判为 `ok=false`。后续报告不能再引用旧 verifier `ok=true` 作为正式 synthetic annotation / geometry 证据。
  - 2026-05-29 scene-level status audit 也已同步收紧：旧 POC03 candidate-only synthetic smoke 现在分类为 `synthetic_candidate_smoke_needs_target_selection`，`synthetic_annotation_ready_count=0`、`synthetic_depth_points_ready_count=0`；后续状态汇总不能再沿用早期 `synthetic_annotation_and_depth_geometry_ready=2` 作为当前 formal readiness。
  - 2026-05-29 target-selection readiness gate 已进一步纳入 controlled actor calibration 与 close-view alternate decode evidence：普通 actor 可由 `green_plus_256_blue` 解码，但 POC03 actor `24` 仍没有在 close ROI / tested PNG decodes 中出现；这强化了 blocker，不是 formal conversion 放行。
  - 2026-05-29 通用 mask -> tracklet -> embedding 链路已补 provenance，并新增显式 `--require-tracklet-lineage` 强制校验模式：可用于后续 isolated smoke 检查真实 mask 输入是否被原地替换，但不得绕过 CARLA-Air formal writer/verifier 或当前 target-selection / NeoVerse / benchmark gate。
  - 2026-05-29 semantic-lidar actor points candidate verifier 已补 scene lineage guard：可防止 actor-relative points root 与 scene/timestamp/pose 错配后仍被诊断链路消费，但不得把 semantic-lidar actor-relative points 叙述为 fixed-camera geometry、NeoVerse reconstruction 或 final real 4D geometry。
  - 2026-05-29 semantic-lidar actor-local geometry candidate 已能把 actor-index points 转为 actor-local coordinate frame，并通过独立 verifier；这增强 actor-level geometry consistency evidence，但仍不是 fixed-camera capture geometry、camera pixel mask、NeoVerse reconstruction 或 final real 4D geometry。
  - 2026-05-29 procedural identity permission POC 已把 local technical multi-identity roster 推进到 4 个 identity，且 `procedural_delta_uav_v1` 具备 project-owned permission evidence；但 `benchmark_eligible_count=0`、`benchmark_roster_count=0`、`ue_carla_verified_identity_count=0`，因此仍不能运行或宣称 formal multi-identity benchmark。
  - 2026-05-29 procedural twinboom identity POC 已把 local technical multi-identity roster 推进到 5 个 identity，且两个 procedural identity 具备 project-owned permission evidence；但 `benchmark_eligible_count=0`、`benchmark_roster_count=0`、`ue_carla_verified_identity_count=0`，因此仍不能运行或宣称 formal multi-identity benchmark。
  - 2026-05-29 procedural canard identity POC 已把 local technical multi-identity roster 推进到 6 个 identity，且三个 procedural identity 具备 project-owned permission evidence；2026-05-30 private/public policy split 后，6 个 normalized identity 均可进入 private local benchmark roster，但 `benchmark_eligible_count=0`、`benchmark_roster_count=0`、`ue_carla_verified_identity_count=0`，因此仍不能运行或宣称 public/formal multi-identity benchmark。
  - 2026-05-29 UE/CARLA import smoke plan 已把 6 个 local POC identity 的未来 editor-import evidence 拆成 per-identity checklist，并由 verifier 确认 non-promotion 边界；但当前 `runnable_now_count=0`、`blocked_now_count=6`，没有实际 UE/CARLA import smoke report，因此不能 patch `build_status.json`、不能增加 `benchmark_eligible_count`、不能运行 formal benchmark。
  - 2026-05-29 UE import smoke plan -> applier contract bridge 已同步到 `research/reports/carla_air_ue_import_smoke_plan_applier_contract_bridge_2026_05_29_zh.md`；bridge 结果保持 `plan contract bridge ok=false`、`runnable_now_count=0`、`blocked_now_count=6`、`formal_benchmark_ready=false`，`verifier ok=true`、`failure_count=0`、`verified_identity_plan_count=6`、`non_promotion_verified=true`，applier template-copied negative rejects `template_contract_marker_present` / `editor_command_not_recognized`，且 `benchmark_eligible_count=0`、`ue_carla_verified_identity_count=0`、annotation / proxy / final geometry unchanged。
  - 2026-05-29 新增 `tools/carla_air/verify_benchmark_promotion_offline_regression.py` 与报告 `research/reports/carla_air_benchmark_promotion_offline_regression_2026_05_29_zh.md`；主审 `local/carla_air/tmp/benchmark_promotion_offline_regression_main_review_blocked_20260529.json` 为 `ok=true`、`failure_count=0`、`expect_blocked=true`、`formal_benchmark_ready=false`、`can_plan_multi_identity_roster_now=false`、`goal_complete=false`、`non_promotion_verified=true`。route-positive 仅是 fixture-only，不能据此宣称真实 benchmark ready；当前仍是 `benchmark_eligible_count=0`、`ue_carla_verified_identity_count=0`，formal multi-identity benchmark 仍阻塞。

next_action:
  1. 先实现 Dataset Generation Pipeline V1 的 plan-only/preflight 层：读取 6 个 normalized identity、固定 Town10HD `node01-node05`、trajectory/camera-layout 配置，并生成统一 dataset plan 与预计样本统计。
  2. 将真实模型替换 gate 作为外部机器交付项跟踪：每个 identity 在进入 full live dataset generation 前必须有 UE/CARLA import smoke 与 runtime spawn/readback evidence；当前 `ue_import_tested=false` / `ue_carla_verified_identity_count=0` 时，本机只输出 blocker 并继续离线 verifier / schema / index 工作，不能在本机尝试无证据的 UE4Editor 构建消耗，也不能宣称 6-model live dataset complete。
  3. 设计并实现统一 training index builder：面向未来 two-camera-node deployment，生成 `train`、`val_in_domain`、`test_cross_layout` 与 camera-pair episode 索引；不要只按随机 frame 切分。
  4. 对现有 capture/scene skeleton 先跑 `mask_gt` availability audit，记录 simulator 是否能提供可信真值；在审计结论出来前，不引入 pseudo mask / SAM / detector / proxy mask 作为主流程字段。
  5. actor-to-pixel、NeoVerse/final geometry、formal benchmark 仍保留为后续分支；只有在 dataset generation preflight/index/mask audit 与对应 readiness gate 明确通过后，再进入 formal conversion、NeoVerse 或 benchmark。

acceptance_criteria:
  - 默认入口 `docs/goal.md`、`research/CURRENT_STATUS.md` 与本计划均指向 Dataset Generation Pipeline V1。
  - plan-only 输出能列出 6 identities、Town10HD、`node01-node05`、trajectory/camera-layout 矩阵和预计样本量。
  - import/readback preflight 能明确阻止当前缺 UE/CARLA evidence 的 full live dataset 完成声明。
  - 统一训练索引能表达 identity、trajectory、camera layout、node/camera、timestamp、RGB/depth/pose/calib 引用和 deployment-oriented split。
  - `mask_gt` 审计能给出可得/不可得/blocker 结论；不可把 proxy / candidate / pseudo mask 记作 `mask_gt`。

out_of_scope:
  - 不把 mp4、review overlays、review_summary 当 pipeline 输入。
  - 不把 presence gate smoke 结果当正式 bbox/mask/object pose 标注。
  - 不把自定义 aircraft 替换或 normalized asset 伪造当作当前采集主线完成条件。
  - 不把缺 UE/CARLA import/readback 的 normalized identity 当作已完成真实模型切换。
  - 不把本机 UE4Editor 编译、Windows/NTFS 盘上的 Unreal 源码修复或手动编辑器导入作为当前本机长任务主线；这些改为外部机器 import/readback 证据交付。
  - 不把单条直线 POC 轨迹当作最终轨迹策略；后续轨迹要以 coverage-first 和 deployment-oriented split 为默认原则。
  - 不把大规模真实场景部署、真实弱标定或端到端 ReID 指标提升当作本阶段冻结验收。
  - 不把 cross-node benchmark 指标或论文结论作为当前 CARLA-Air freeze 的完成条件。

latest_retrospective: research/reports/carla_air_goal_reset_dataset_generation_pipeline_2026_05_30_zh.md
must_read: docs/goal.md
last_updated: 2026-05-30
